# Human/agent parity audit

Last audited: 2026-08-12

## Verdict

AIMuse has practical parity for the current canonical alpha loop: an authenticated agent can create/configure a Song or SFX project, inspect exact state and attached-editor hints, apply granular revision-checked edits, audition bounded renders, manage jobs, checkpoint/branch, and undo/redo without an editor. A human can attach and edit the same project with priority locks and visible attribution.

Parity does not imply v1 DAW completeness. Managed-preview WASAPI shared playback is available to both Windows surfaces, and the shared CoreAudio runtime now has bounded native development evidence on macOS. The exact macOS editor/MCP bidirectional workflow has not been independently certified. Live-graph/exclusive CoreAudio, recording/MIDI, deep audio gestures, complete mixer/automation, SDK-backed plug-ins and several observation modalities remain incomplete or absent. Some asymmetries are intentional security or responsiveness boundaries.

## Shared canonical boundary

Renderer IPC and MCP commit through `ProjectService`, the same operation schema/reducer, per-project mutation queue, two-phase native graph handshake, journal and trace. Authenticated MCP direct/branch transactions, actor history, checkpoint/variant create/merge/discard, generation start/variation/accept/reject, plug-in parameter/preset/bypass/remove and WAV media analysis additionally share a fair bounded external-agent admission layer; human renderer commits, read-only variant comparison, generation capabilities/inspection, plug-in catalog and media listing do not consume those lanes. Server-owned policies normalize actor identity, timestamps, revisions and trusted generation provenance. Agents receive semantic music operations rather than replacing tracks or reimplementing quantize/humanize/transpose/legato/arpeggiate algorithms.

## Closed parity gaps

| Area | Current contract and evidence |
| --- | --- |
| Project creation | MCP and editor create Song/SFX projects against the same schema and defaults. |
| State observation | Snapshot/diff includes canonical tracks/clips/devices/automation/assets/provenance plus optional selection/transport/locks. |
| Collaboration | Human entity/time locks conflict with agent edits; unrelated work proceeds and retry guidance is returned. |
| Canonical corruption boundary | Human and agent service paths share the same reducer rejection of malformed SFX-deliverable, media-asset, generation-provenance, checkpoint and variant entity metadata at stored and existing operation boundaries, completing coverage for every currently declared `EntityBase` family. Generation provenance also shares strict-schema validation for its immutable asset/reference IDs, provider/model/kind/prompt/lyrics/request/cost/currency/rights fields and experimental flag before lookup or mutation. Devices share strict-schema validation for track-ID, format, optional built-in kind and plug-in ID/version/hash/vendor descriptors before relationship lookup, mutation or direct/cascade deletion. Automation shares strict-schema validation for lane track/target/point-index/state shapes and point nonnegative safe-integer tick/value/curve/tension fields before mutation or direct/device/track cleanup. Server normalization still supplies trusted attribution/timestamps after validation; repeated valid provenance references, empty optional strings, length-bounded non-digest plug-in hashes, independent device descriptors, explicit automation create order and chronological point-upsert order remain schema-compatible. This closes input-corruption asymmetries without adding timestamp ordering, collection-key coupling, actor lookup, provider-kind/device-format coupling, reference/request uniqueness, hash/billing interpretation, native hosting, target-specific automation ranges, device-parameter existence, cross-track ownership, curve/tension coupling, repair, retention, cleanup or new lifecycle semantics. |
| Device latency samples | Human and agent canonical device paths now share strict-schema rejection unless `latencySamples` is a nonnegative safe integer across stored integrity, add/update/move/parameter mutation, direct delete and track-cascade cleanup. Existing merged-result update may explicitly replace malformed latency; unrelated mutation and deletion/cleanup reject it. Exact endpoints, device order, attribution, valid-lifecycle inverses and established diagnostics remain compatible. This adds no general repair, measurement, unit conversion, compensation/PDC, DSP, native discovery/hosting, routing, retention or cleanup semantics. |
| Timeline marker ranges | Stored integrity and canonical marker add/update/delete now share strict-schema validation for safe nonnegative ticks and an optional later safe end tick. Update cannot repair a malformed target and deletion cannot hide one. Existing metadata, duplicate/text/revision/order diagnostics, exact endpoints, equal ticks, reverse explicit order, kind/range independence, attribution and inverses remain compatible without chronology, uniqueness, total-range coupling, containment, overlap, snapping, migration, repair, retention or cleanup policy. No compound/split/cascade marker path exists. |
| Song-section ranges | Stored integrity and canonical section add/update/delete now share strict-schema validation for required safe nonnegative endpoints and the established non-empty forward range. Update cannot repair a malformed target and deletion cannot hide one. Existing metadata, duplicate/text/revision/order/energy diagnostics, exact endpoints, identical overlapping ranges, reverse explicit order, name/energy independence, attribution and inverses remain compatible without chronology, sorting, uniqueness, total-range coupling, containment, overlap, snapping, arrangement, lifecycle, migration, repair, retention or cleanup policy. No compound/split/cascade section path exists. |
| Shared clip-base numerics | Stored integrity and canonical audio/MIDI clip add/update/move/trim/split/delete or track-cascade paths now share strict-schema validation for safe start/duration integers, finite `-120..24` gain, safe fade durations and optional positive safe loop length; take-lane cleanup retains the same final integrity postcondition. Direct move/trim/split rejects malformed values. Existing diagnostics, independent exact endpoints, ordering, attribution and inverses remain compatible without chronology, total-range overflow, overlap, containment, snapping, fade/loop cross-field coupling, musical interpretation, migration, repair, retention or cleanup policy. |
| Audio clip source descriptors | Stored integrity and canonical clip add/update/move/trim/split/delete or track-cascade paths now share strict-schema validation for a shaped existing asset reference, nonnegative/positive safe-integer source start/duration and finite `-48..48` transpose; take-lane cleanup retains the same final integrity postcondition. Direct trim rejects rather than normalizes malformed sample input. Existing diagnostics, exact boundaries, repeated references, ordering, attribution and inverses remain compatible without media-kind/duration coupling, containment, marker partitioning, ownership, migration, repair, retention or cleanup policy. |
| Audio warp-marker declared values | Canonical stored integrity and every existing marker-carrying clip add/update/move/trim/split/delete or track-cascade path now share strict-schema rejection for source samples and project ticks outside the nonnegative safe-integer range. Operation metadata precedence and stored per-marker ordering remain stable; exact endpoints, duplicate IDs, explicit order, reverse chronology, attribution and inverses stay compatible. No chronology, uniqueness, containment, source-duration coupling, split partitioning, migration, repair, retention or broader cleanup policy is inferred. |
| Comp-segment declared values | Canonical stored integrity, upsert/replacement, direct deletion and take-lane cleanup now share strict-schema validation for shaped track/take-lane IDs and nonnegative safe-integer ticks, retaining the existing same-track and non-empty-forward-range rules. Metadata and relationship diagnostics keep precedence; valid exact boundaries, attribution and inverses remain compatible. No overlap, selection, ordering, containment, playback, retention, repair or broader cleanup policy is inferred. |
| Immutable track kind | Human and agent canonical reducer paths now share strict-schema rejection for malformed track kinds across stored integrity, add/update/move/delete and recursive cleanup. All six existing literals, attribution, membership, inverses and established kind-specific behavior remain compatible; prior duplicate-ID, parent, mutable-field, master, reference and dependent-record diagnostics retain precedence. This adds no kind conversion, routing, ownership, retention, lifecycle, repair or cleanup policy. |
| Track hierarchy cycles | Human and agent moves share the same pre-mutation rejection when a folder would become its descendant's child. Schema validation and persisted folder reads also reject a locally reciprocal stored cycle without mutating project state or repairing corrupt bytes. Audio-output/send/device routing cycles, hierarchy deletion/retention and repair/migration policy are not inferred from this hierarchy-only evidence. |
| Actor history | Selective undo/redo changes only values still matching that actor's edit and preserves later work by others. |
| Checkpoints/variants | Agents and humans use the same immutable project checkpoints and named variant service. Authenticated agent create/merge/discard actions use fair bounded admission with non-idempotent re-observation guidance; queued cancellation precedes service and snapshot I/O, while compare remains read-only. |
| Generation lifecycle | Owner-scoped agent start/variation/accept/reject uses the shared fair admission boundary. Queued start/variation creates no approval reservation/job and performs no credential/provider work; queued accept/reject leaves candidates and project untouched. Admitted provider execution remains an owner-only cancellable job outside the mutation lanes. Cancellation stays terminal when an injected provider ignores abort: under installed process authority a valid late payload consumes its fulfilled-request budget unit but cannot start candidate persistence, while a cache write already in flight may retain one immutable owner candidate and still makes no project transaction. Exact provider/cache/project partials and re-observation guidance remain generation-specific. Only later explicit acceptance registers the source and commits attributed asset/provenance/clip state; duplicate acceptance does not recommit. |
| Plug-in device edits | Parameter, preset, bypass and removal actions use shared fair admission at cost one, then commit as one authenticated revision-checked project transaction. Queued cancellation invokes no transaction; admitted work preserves human locks, serialized commit order and atomic project semantics. Catalog, scan jobs and authority-gated instantiate retain separate contracts, so no new native-hosting or approval claim is implied. |
| Plug-in scan jobs | Owner cancellation remains terminal across empty discovery, last-candidate settlement and final atomic persistence. Candidate results are scan-private until one complete globally shared replacement, so cancelled partial work leaves the prior catalog; a cancellation-overlapped resolved write may retain the complete replacement, while rejected atomic persistence settles unchanged. Owner metadata distinguishes discovery/helper/candidate/catalog phases and requires job plus catalog re-observation without automatic retry. The four-test authenticated evidence injects every seam and does not execute or certify the native helper. |
| WAV media analysis | Analysis uses shared fair admission at cost one. Queued cancellation performs no source read, cache output or project transaction. Running analysis writes three content-addressed outputs before one atomic three-asset commit, so failed running work may leave cache artifacts but never a partial project commit. Import and render-job actions retain their distinct authority and multi-effect boundaries. |
| Approved media imports | Exact-path approval precedes the sequential continuation and remains outside fair lanes. Waiting cancellation releases approval capacity with no source/cache/project effect. Running cancellation is terminal but non-preemptive: every file independently reports source read, cache copy, project transaction, asset-source registration and warning state. Resolved cache copies and earlier/later successful transactions remain after a warning; a rejected transaction stays conservative. Direct authority-allowed import remains synchronous/job-free. Owner inspection and project/cache re-observation precede repetition; no request-wide atomicity, cleanup, rollback or retry is implied. |
| Media render jobs | Audition/consolidation job creation uses shared fair admission at cost one. Queued cancellation creates no job, render, cache output or project transaction; the admitted owner-only job does not keep its lane occupied while render remains pending. Running cancellation is terminal but cooperative: a late cache WAV, already-started attributed asset transaction, and already-started consolidation clip transaction may remain, with exact retained effects recorded on cancelled or failed jobs for inspection and project re-observation. No preemption, rollback or request-wide atomicity is implied. |
| Export jobs | Authenticated owner cancellation is terminal across master, stems, MIDI, DAWproject, SFX batch and portable pack. Waiting cancellation writes nothing and releases approval capacity; running cancellation does not preempt output, clean up retained files, roll back a possible pack save or retry automatically. Cancelled/failed jobs expose exact `partial.output` and `partial.project` guidance, and injected master/stems late completion cannot resurrect status. Session DELETE remains separately limited to queued scheduler work and presence. |
| Approved project opens | Waiting cancellation releases approval capacity before source read/workspace/project/controller effects. Running cancellation is terminal without preemption or rollback. Returned opened IDs settle retained/synchronized only when settlement-time project/controller identity confirms both; returned warnings with no opened ID prove unchanged workspace/controller state; rejection or unconfirmed concurrent state stays conservative because project retention precedes synchronization. Direct authorized open remains synchronous/no-job; compound unpack retains the separate contract below. |
| Approved project unpack | Waiting cancellation releases approval capacity before archive access, destination creation, sequential entry writes, validation or open. Running cancellation is terminal but non-preemptive; owner partials preserve bounded per-entry counts, destination/removal outcome and later workspace/project/controller settlement. Validated extraction remains across open warnings/failure, while a failed extraction/removal can leave partial output. Direct authorized behavior remains synchronous/no-job. Destination/workspace re-observation precedes repetition; no archive-wide atomicity, cleanup, rollback, preemption, retry or fair-lane admission is implied. |
| Approved project saves | Waiting cancellation releases approval capacity before destination/project/audit effects. Running cancellation is terminal but does not preempt persistence, clean partial output or retry: late success retains destination output, saved clean state/path and authenticated `file.saved` audit; a rejected continuation conservatively reports possible destination, clean-state and audit effects. Owner inspection, project/audit re-observation and destination inspection precede repetition. Other approved producers keep separate contracts. |
| Approved plug-in instantiate | Waiting cancellation releases approval capacity before authority continuation and project mutation. Running cancellation remains terminal without preempting the single authenticated atomic descriptor-device transaction: a late returned commit remains, a returned human-lock/current-target conflict commits no device, and a rejected continuation reports conservative uncertainty without retry. Destination-track/device locks are checked before reduction, direct allowlist-authorized behavior remains synchronous/job-free, and the listener evidence starts no native host or helper. Catalog/scan, SDK-backed instantiation, fair admission and native plug-in execution remain separate. |
| Auditory feedback | Bounded audition/stem renders are addressable MCP media resources with engine-owned asset metadata. The same real non-silent 32-bit-float audition is visible and playable in the packaged editor through the stream-enabled custom media scheme. |
| Real-time transport | Editor IPC and MCP transport use the same background-prewarmed revision preview and native playback state. Windows has packaged WASAPI cross-surface evidence; macOS has bounded native shared-CoreAudio callback evidence but not yet formal editor/MCP parity evidence. Preview rendering does not block Electron, revision swaps preserve cursor/play state, and both surfaces share the same transport contract. |
| Throughput | Per-project serialized mutation tails prevent simultaneous UI/MCP lost updates. Agent direct/branch transactions, actor undo/redo, checkpoint/variant create/merge/discard, generation start/variation/accept/reject, plug-in parameter/preset/bypass/remove, WAV media analysis and audition/consolidation job creation share four fair bounded admission lanes; free capacity starts immediately and actor round-robin applies once queued. Eight focused files / seventeen tests cover the family-specific cases plus a bearer-authenticated eight-seed mixed soak and generation late-fulfillment matrix. Eight independent soak executions prove four-lane saturation, actor and exact 2,048-cost global pressure, project/session queued cancellation, observation bypass, human-lock priority, attribution, contiguous commit order and 24 edits proceeding while injected provider/render jobs remain pending. Closing a session guarantees queued-effect cleanup, not delivery on its closed transport. No fixture starts audio/native helpers or external network; packaged acceptance remains separate. |
| Editor control integrity | Enabled renderer buttons are mechanically audited for an action, high-frequency draft controls serialize and rebase canonical commits, and action failures surface in the editor instead of looking inert. Headless Chrome covers the main toolbar, timeline tools, devices, mixer, automation, candidate audition and agent connection; exact packaged QA additionally covers Windows Escape normalization, tab-scoped search, global history after blur, native dirty Cancel/Save and real audition playback. Historical hash `E558…412` passed authorized disposable-project Discard; current hash `CFCC…30621` did not repeat that destructive action without action-time confirmation. |
| Attribution/provenance | Server actor/client metadata is durable; generated provenance is engine-only and candidates remain outside the arrangement until acceptance. Human and authenticated-agent saves carry their actor through a separate durable destination-free `file.saved` audit without changing content revision or undo history; an approved save that finishes after owner cancellation retains the same authenticated actor. |

## Remaining parity backlog

| Tracker | Gap | Why it matters |
| --- | --- | --- |
| AGT-02/QA-04 | Subscription and complete action acceptance | Agents need proven live observation across every public resource/tool action. |
| AGT-10 | Musical three-way merge depth | Current project-level variants need selective entity/range conflict review. |
| AGT-16/GEN-09 | Spectrogram/key/transient and synchronized before/after | Autonomous iteration needs richer auditory/visual comparison. |
| SONG-02–12 | Editor semantic completeness | Fold/split/MIDI draw, device insertion and initial automation-lane creation now commit through the canonical operation surface; deeper audio, piano-roll, mixer, automation and hierarchy workflows remain thinner or model-only. |
| PLG-06/07 | Plug-in descriptors versus native UI | Agent parameter/preset/bypass/remove edits now have fair admission over stable IDs; host discovery, automation/preset round-trip and managed human native editor windows remain. |
| AUD-02/UX-02 | Live graph, exclusive endpoints and callback telemetry | Shared preview playback now refreshes revisions without blocking or rewinding; both surfaces still need endpoint controls, hot-plug behavior, true streaming graph updates, meters and xruns. |
| QA-09/10 | Higher-level packaged certification | Independent Luna/high Level 1 passed for Windows exact hash `4CF335C1…B16EC7`, including exact-URI subscriptions, both cross-surface directions, native history, cleanup and redaction with no P0–P3 finding. Separate Windows hashes remain retained and scoped. The local macOS package/headless evidence is not transferable certification; a fresh independent task must still prove human→agent and agent→human behavior with native Computer Use on the exact Mac package. Formal Levels 2/3 remain; provider generation stays an explicit unconfigured/no-paid-call exception. |
| REL-04/06 | Dependency/build-chain security versus packaged certification | Electron is pinned to registry-current supported `43.4.0`; fresh production and explicit complete-tree audits are clean, and the release gate cannot omit build tooling. Patched ZIP/TAR/tmp substitutions execute through existing consumers, while 743 registry signatures and 136 attestations verify. A temporary checksum-verified official CMake `4.4.2` toolchain carried the ordinary arm64 development package through fresh native build, Forge, hardened-fuse/layout/hash verification and a valid ad-hoc signature; the embedded Electron framework reports `43.4.0`. This does not certify a release artifact: no committed immutable subject, maker/install cycle, SBOM/license bundle, Developer ID/notarization or formal release evidence was produced. |

## Boundary asymmetries by design

| Agent | Human |
| --- | --- |
| Bounded operation count, explicit transaction idempotency, fair project/history/checkpoint/generation/plug-in-device/WAV-analysis admission, owner-only generation jobs, and inspection/re-observation before repeating non-idempotent actions; coarse locks/advisory UI state | Pointer previews remain local, human commits do not consume external-agent lanes, and human edits have priority inside an active gesture |
| Addressed paths and policy/approval for reads, writes and every overwrite | Native dialogs carry direct one-time intent |
| Provider/model/budget authority and approval for unknown-cost requests | Human-started generation does not ask a second agent-authority question |
| No microphone/MIDI by default and no inline media bytes | Direct UI recording still requires OS/user permission and explicit arm/record action |
| Plug-ins by stable descriptor/preset/parameter ID | Managed native plug-in editors are a human-only UI surface |

These boundaries must remain visible and tested; parity work must operate within them rather than remove them.

## Maintenance rule

Any material change to human or agent capabilities updates this audit and [FEATURE_TRACKER.md](FEATURE_TRACKER.md) together. “Working” means usable now; “Verified” requires automated acceptance evidence. Formal release confidence additionally requires the independent [three-level workflow](TESTING.md).
