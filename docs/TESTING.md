# AIMuse testing workflow

Last updated: 2026-08-07

This workflow is adapted directly from AIDraw and defines three formal test levels for AIMuse. Every formal run uses a **new independent Codex task configured as `gpt-5.6-luna` with `high` reasoning**. The implementation task does not certify its own work.

Passing automated tests alone is insufficient. Every formal level includes:

1. source-level automated checks;
2. authenticated isolated MCP behavior;
3. native Windows UI interaction through the Computer Use plugin; and
4. at least one assertion in each direction: MCP change visibly verified in the UI, and Computer Use UI change verified through MCP.

Playwright is valuable automated coverage, but it does not replace Computer Use. Computer Use targets the real AIMuse window and exercises native menus, dialogs, focus, pointer placement, transport/editing controls and visible rendering.

## Roles and invariants

- The primary task implements changes, updates `FEATURE_TRACKER.md`, and selects the required level.
- A **new** Luna/high task performs each formal run. Do not reuse the implementation task as tester.
- On Codex desktop, the primary task is only the mechanical launch coordinator: it starts/shows/stops the tester's declared isolated package outside the filesystem sandbox and does not judge cases.
- The tester is read-only for production source. It may write ignored reports and evidence below its run root, but must not fix code, soften assertions or change tracker status.
- Test projects use `QA L<level> · <UTC run ID> · <description>`. Never mutate, overwrite, close or discard a pre-existing user project.
- Every level uses the exact newly packaged executable and isolated profile. A convenient pre-existing AIMuse window is not a formal subject.
- Packaging and formal QA use Node 24.x. Runtime preflight and post-package executable/native/fuse verification must pass; a Forge exit code without verified artifacts is failure.
- Paid provider requests are forbidden. Generation is tested with mocks, preflight rejection, cancellation or denial.
- A missing window, unavailable Computer Use helper, unavailable MCP connection or unknown/mismatched build identity is `BLOCKED`, not Pass.
- Confirmation-required destructive actions require explicit authorization. Without it, stop at the confirmation boundary. An authorized Discard check may target only a disposable run-owned dirty project; never overwrite, close, discard or delete pre-existing user data for a test.

## Level overview

| Level | Name | When | Automated command | Formal result requirement |
| --- | --- | --- | --- | --- |
| 1 | Smoke | Focused feature/bug-fix handoff | `node scripts/npm-node24.mjs run test:level1:auto` | Core automation, one MCP edit loop, one native UI edit loop and both cross-surface assertions pass. |
| 2 | Regression | Feature cluster or milestone | `node scripts/npm-node24.mjs run test:level2:auto` | Full fast/native/package/Playwright suite plus core Song, SFX, persistence, collaboration and lifecycle regressions pass. |
| 3 | Release exhaustive | Every release candidate | `node scripts/npm-node24.mjs run test:level3:auto` | Release artifacts plus exhaustive MCP, Computer Use, audio, interchange, recovery, security, performance and accessibility evidence pass. |

The `:auto` commands are only the automated portion. The independent MCP + Computer Use report remains mandatory.

## Current formal evidence

The newest independent Luna/high Level 1 run at [`20260806T025127Z-post-gen06-safe-storage-level1`](../test-results/luna-high/20260806T025127Z-post-gen06-safe-storage-level1/report.md) is `BLOCKED`, not FAIL, for exact executable SHA-256 `9F82CC2C52E5188B636237165B5F849F879CB540F6131227A3CFF736F6AA758C`. Launch-free Node/renderer 162/162 plus 2/2, native CTest 1/1 and exact package/layout/fuses passed; the coordinator then launched the isolated profile and verified PID 492, instance/profile/health/native-audio identity and an exact AIMuse window handle. Computer Use exact-handle rehydration failed before MCP initialization, input, mutation or any test credential, so real provider `safeStorage`/DPAPI, setter/capability refresh and live Electron leakage remain unearned rather than failed. No P0–P3 product finding occurred. Cleanup completed `stopped-redacted`: PID/exact-package process count zero, connection/session credentials redacted, hash unchanged, no force termination and the Computer Use lease revoked.

The newest independent Luna/high Level 1 run at [`20260805T191949Z-post-mcp-owner-audit-discovery-level1`](../test-results/luna-high/20260805T191949Z-post-mcp-owner-audit-discovery-level1/report.md) is `BLOCKED`, not FAIL, for exact executable SHA-256 `9456A999AF1DEA3910A3AA81D42E0CD136C721957038E6E3F9138CDBB69B32BC`. Node/renderer 154/154 plus 2/2, native CTest 1/1, exact package/layout/fuses, cold `tools/list`, `aimuse_help`, invalid-combination rejection, two-session owner-only job/resource privacy and out-of-root no-write/cancel passed; no P0–P3 product finding was observed. Although coordinator show, PID, hash, title and native handle agreed, the Computer Use bridge could not expose a safe PID-bound target. UI approval, authorized in-root save, durable `file.saved` and its revision/activity/undo invariants are therefore unearned, not failed. Coordinator cleanup completed `stopped-redacted`: PID 45844 and exact-package process count zero, connection/tester credentials redacted, executable hash unchanged, both save targets absent and no force termination. This result is hash-scoped evidence but not a Level 1 certificate.

Post-run GEN-06 source separates the Electron protected-storage adapter from the testable credential store and uses validated same-directory atomic replacement for rotation/removal. Six mock-only temporary-directory cases prove successful/interrupted rotation/removal plus unavailable, throwing-encryption and throwing-decryption behavior: known-good bytes and unrelated providers survive, stages are cleaned, errors contain no fixture secret, capabilities remain unconfigured and generation makes zero provider fetches. Two further headless cases parse the production desktop/preload/main contract and exercise the real project/generation/MCP paths over authenticated loopback: the frozen preload remains provider-setter-only with an acknowledgement-only result, distinctive plaintext and stored-ciphertext sentinels stay out of bootstrap snapshots, capability/status output, workspace events, public MCP tools/resources, errors/logs and serialized renderer-visible state, and a mock provider diagnostic echoing the active credential is redacted before failed-job publication. The complete package workflow passed 26 Vitest files / 162 tests, clean TypeScript/ESLint, renderer 2/2 and native CTest 1/1, producing immutable executable `test-results/package-prep/20260806T024716Z-post-gen06-credential-leakage/AIMuse-win32-x64/AIMuse.exe`, SHA-256 `9F82CC2C52E5188B636237165B5F849F879CB540F6131227A3CFF736F6AA758C`. Its independent result is the infrastructure-BLOCKED run above, not a certificate: real provider DPAPI/live Electron IPC evidence remains unearned, while no product failure was observed.

The independent Luna/high Level 1 run at [`20260805T064606Z-post-mcp-resource-subscriptions-level1`](../test-results/luna-high/20260805T064606Z-post-mcp-resource-subscriptions-level1/report.md) is `PASS` for exact executable SHA-256 `4CF335C1787F91C036F17FE3AA4A2F6309463F231D306A688B86A43633B16EC7`. Node/renderer 149/149 plus 2/2, native CTest 1/1, exact package/layout/fuses, authenticated exact-URI split delivery/unsubscribe/DELETE/replacement ownership, MCP idempotency/history, real pointer MIDI/mixer edits, Ctrl+Z/Ctrl+Y, editor surfaces, WASAPI and both cross-surface directions passed with no P0–P3 finding. The report intentionally ends at `TEST_COMPLETE_AWAITING_COORDINATOR_STOP`; coordinator cleanup then returned `stopped-redacted`, proved PID 46052 and all AIMuse processes absent, preserved the hash, redacted connection/session/MCP evidence with no token/Bearer material and revoked Computer Use. Original `New Song` was restored unchanged; two isolated run-owned QA projects remain retained. Provider generation remains an explicit unconfigured/no-paid-call exception.

The `9456A999…B69B32BC` report records that the unchanged native command passed via a coordinator-owned genuine out-of-sandbox rerun. The composite package command was not rerun over that prepared subject; its exact bytes were preserved. The initial sandbox-denial provenance and later escalated success for retained `58E232A3…ED476` remain recorded separately in its own report/current-truth history.

The prior [`20260805T052028Z-post-qa10-level1`](../test-results/luna-high/20260805T052028Z-post-qa10-level1/report.md) PASS remains separate evidence for executable SHA-256 `58E232A387C8625B8E9CD84C2DB2EA2CD82D09D9DFCC5AC8B02C1C1A3ABED476`, including stopped-redacted cleanup and no P0–P3 finding. [`20260805T021027Z-post-fnd02-level1`](../test-results/luna-high/20260805T021027Z-post-fnd02-level1/report.md) separately certifies `CFCC482C04C21124D82E857428B561DB5FAB60A3A61390E8535471E99E830621`; Discard was not run because action-time confirmation was unavailable. [`20260805T012342Z-level1`](../test-results/luna-high/20260805T012342Z-level1/report.md) historically certifies `E558EA355F7AB40391BC38FD918ECFB418451F39C0DE520B58BCE8ABE99EC412`, including authorized disposable-project Discard. All certificates remain executable-hash-scoped and separate.

Post-certification headless QA-session fault coverage requires the coordinator harness to fail before process access when executable identity or evidence paths are invalid, and requires redaction to rebuild public PID/URL state from the validated manifest rather than preserve connection-file identity or credential fields. This coverage strengthens future runs but does not retroactively alter either certified executable.

Post-certification QA-04 coverage asserts all four concrete resources, all six templates and every public project/session/catalog/guide/manifest/snapshot/change/trace/job/binary-media read from isolated fixtures, plus per-session exact-URI update delivery. Two authenticated persistent SSE sessions split manifest/snapshot/change subscriptions, prove no cross-session leakage, fence unsubscribe suppression against another subscribed delivery, close a subscribed session/stream and prove a clean replacement owner. Job tools, generation job actions, `aimuse://sessions` and `aimuse://jobs/{id}` are owner-scoped: a foreign actor gets the same missing result/error, sees no private sentinel and cannot mutate the job. Another isolated two-session contract holds human entity/time locks while conflicting operations remain non-mutating and unrelated work commits with attribution.

The two-session file-authority slice now passes. An out-of-root save creates exactly one owner-only waiting approval job and neither the requested path nor its `.aimuse` form; the foreign session discovers no job ID/request/path through tool or resource reads, while the owner receives human-only approval and bounded polling guidance. A second authenticated session saves inside its run-owned approved root, and the durable `activity/file-audit.jsonl`/`project_observe` record retains only `file.saved`, project, server actor, timestamp and successful outcome. Project revision, reducer activity and actor undo state do not change; agent and human saves round-trip. Cold-client discovery additionally verifies exact action `oneOf` requiredness, important field/output descriptions, initialize workflow, the model-callable help tool, complete optional guide, job/approval next steps and rejection of invalid action combinations. This contract remains part of the current 25-file / 160-test source baseline.

Post-AUD-02 QA-01 source adds one isolated automation-family integrity/inverse contract. Exact selected Vitest execution passes 2/2: lane/point add, upsert, tick reorder, update and delete retain actor attribution and inverse semantic state, while orphaned/duplicate track ownership, incomplete point order and missing device targets fail integrity validation. TypeScript and exact-file ESLint pass. No aggregate suite, native/audio/device, application or package path ran, so the earlier aggregate count remains historical rather than evidence for these changed bytes.

The next isolated QA-01 device-routing contract also passes 2/2. Device add/update/parameter/move/delete, send and sidechain upsert/update/delete, dependency cascade and every corresponding inverse preserve semantic state. Update upserts now retain original creation actor/time and attribute the update separately. Integrity rejects orphaned/duplicate/unsupported-track devices, missing state media and missing send/sidechain endpoints. Its inspected core-only graph contains no child process, native/audio/hardware, app/package, provider/credential or network path; the exact single-file Vitest run, TypeScript and exact-file ESLint pass without an aggregate claim.

The isolated QA-01 media/history contract passes 2/2, and the prior two files pass 4/4 again against the same final reducer. Checkpoint registration now requires snapshot media, variant snapshot updates require existing media, variant snapshots explicitly block asset deletion, and integrity rejects missing checkpoint/provenance/variant references. In-memory asset, provenance, checkpoint and variant register/update/delete subsets preserve semantic state on inverse and actor attribution. The inspected graph has no filesystem fixture or other forbidden runtime boundary. Deleting a checkpoint referenced by merged/discarded variants remains intentionally undecided: the operation mentions active variants, but the model requires `baseCheckpointId` for every status and declares no variant deletion. No cascade, tombstone or dangling reference policy is inferred.

The isolated QA-01 take-lane/comp-segment contract passes 2/2, and all four exact pure-core files pass 8/8 against the final reducer. Integrity now requires unique reciprocal clip/take-lane membership on one existing track and comp segments with an existing same-track lane plus a non-empty forward range. Comp upsert retains its original creation actor/time while attributing later updates. In-memory lane update/delete, clip delete, segment upsert/update/delete, dependency cleanup and every corresponding inverse restore semantic state. TypeScript and exact changed-file ESLint pass. Track deletion retention for take lanes/segments, active-lane and comp overlap/selection rules, media storage and collaboration behavior remain undeclared and unchanged; the earlier self/cyclic-send, cross-track automation and checkpoint-retention choices also remain open.

The isolated QA-01 tempo/meter/marker/section contract passes 2/2, and all five exact pure-core files pass 10/10 against the final reducer. Tempo and meter indexes now require complete duplicate-free record membership, chronological ticks and at least one tick-zero anchor; marker and section indexes require complete duplicate-free membership but intentionally retain explicit user order rather than being sorted by time. Supplied marker ranges and every section are forward/non-empty, and declared section energy remains finite within 0–1. Tempo/meter updates retain original creation actor/time while attributing the updater; event/marker/section add, update and delete operations restore semantic state on inverse, and tick-zero tempo/meter deletion remains fail-closed. Equal-tick precedence, marker-kind endpoint requirements, overlap, snapping, musical interpretation, editing UX and retention policy remain undeclared and unchanged.

The isolated QA-01 track-hierarchy contract passes 2/2, and all six exact pure-core files pass 12/12 against the final reducer. `trackOrder` is now the exact duplicate-free root set; every nested track occurs exactly once in one folder's explicit child order with reciprocal `parentId`; the master remains root; missing, duplicate, non-folder, non-reciprocal and cyclic hierarchies fail integrity validation. Root/nested add, root-to-child, child-to-child and child-to-root moves, sibling reorder and subtree preservation restore semantic state on inverse while retaining creation attribution and recording the mover. Cascade deletion was not exercised or changed. Routing remains existence-checked only; routing-cycle policy, allowed depth, naming, hierarchy UX, solo/mute propagation and retention semantics remain undecided.

The isolated QA-01 unlaned-clip contract passes 2/2, and all seven exact pure-core files pass 14/14 against the final reducer. Each track now has duplicate-free explicit clip order reciprocated by every clip's `trackId`; clip kind must be accepted by its track; declared timeline, gain, fade, loop, audio source and warp-marker numeric bounds fail closed. Cross-kind split payloads are rejected before mutation. In-memory MIDI/audio clip add, update, compatible cross-track move, trim, MIDI split and delete restore semantic state on inverse while retaining creation attribution and recording the editor. Clip order intentionally remains independent of timeline position. Overlap, snapping, take-lane reassignment, audio split source partitioning, asset-length/source containment, media storage, clip UX and retention semantics remain undeclared and unchanged.

The isolated QA-01 MIDI note/control/pitch-bend contract passes 2/2, and all eight exact pure-core files pass 16/16 against the final reducer. Note, control and bend order arrays now require exact duplicate-free map membership and matching map-key/entity IDs. Notes enforce declared start/duration/pitch/velocity/release/channel/probability bounds; controls enforce tick/controller/value/channel bounds; bends enforce tick/value/channel bounds. In-memory note add/update/delete, control add/delete and pitch-bend add/update/delete restore semantic state on inverse while retaining creation attribution and recording the editor. Event arrays intentionally preserve insertion rather than chronological order. No control-update operation is declared, so none was invented. Clip containment, note overlap, event chronology, quantize/humanize and every other semantic transformation, musical interpretation and editing UX remain unchanged and outside this slice.

The isolated QA-01 SFX-deliverable contract passes 2/2, and all nine exact pure-core files pass 18/18 against the final reducer. Deliverables now enforce their declared text/format, timeline/variant, loop-point numeric, tail/variation and loudness ranges. In-memory add, update and delete restore semantic state on inverse while retaining creation attribution and recording the editor. Existing structural loop-point pairing and forward ordering remain fail-closed, but no relationship to `seamlessLoop` or rendered samples is inferred. Rendering, codec behavior, naming-template expansion, loop interpretation, storage, export execution, UX and retention policy remain unchanged and outside this slice.

The isolated QA-01 project metadata/settings contract passes 2/2, and all ten exact pure-core files pass 20/20 against the final reducer. Integrity now enforces the declared project name length/kind and settings sample-rate/channel-layout/PPQ/record-depth literals, count-in integer range, metronome boolean, nonnegative integer crossfade and finite loudness range. In-memory rename and settings updates restore semantic state on inverse, rename retains its existing trim behavior, and the authenticated transaction actor remains attributed in activity. The existing settings-edit crossfade ceiling remains an operation rule rather than being promoted into the broader stored-project schema. Product defaults, migration, compatibility, UX, persistence, collaboration and release semantics remain unchanged and outside this slice.

The isolated QA-01 lyrics contract passes 2/2, and all eleven exact pure-core files pass 22/22 against the final reducer. Both reducer operations and integrity now accept the declared 200,000-character boundary and fail closed beyond it rather than silently truncating. Exact whitespace/newlines are preserved; in-memory set, replace and clear operations restore semantic state on inverse and retain authenticated transaction attribution. No structure, interpretation or normalization was inferred. Authoring, synchronization, storage, collaboration, UX, compatibility and release semantics remain unchanged and outside this slice.

The isolated QA-01 track mutable-field contract passes 2/2, and all twelve exact pure-core files pass 24/24 against the final reducer. Direct reducer updates and integrity now enforce declared track name/color lengths, finite gain/pan ranges, boolean state flags, routing monitor values, syntactic output IDs, device-ID lengths and nonnegative integer input-channel lists. One attributed `track.update` across every mutable field preserves creation metadata and restores semantic state on inverse; exact minimum/maximum values pass. Existing output existence/cycle and hierarchy checks remain separate. Routing-cycle, hierarchy, deletion/cascade, naming, solo/mute propagation, authoring, storage, collaboration, UX, compatibility and release policy remain unchanged and outside this slice.

The isolated QA-01 marker/section textual contract passes 2/2, and all thirteen exact pure-core files pass 26/26 against the final reducer. Marker add/update/integrity now enforce declared name/color lengths and marker/region/cue kind values; section add/update/integrity enforce declared name/color lengths and the optional 10,000-character prompt bound. Attributed add, update and delete lifecycles preserve creation metadata and restore semantic state on inverse, and updates leave explicit marker/section order unchanged. A schema-valid region without `endTick` remains accepted because no kind/endpoint relationship is declared. Arrangement, overlap, endpoint, snapping, musical interpretation, naming, authoring, collaboration, storage, UX, compatibility and release policy remain unchanged and outside this slice.

The isolated QA-01 tempo/meter value contract passes 2/2, and all fourteen exact pure-core files pass 28/28 against the final reducer. Tempo upsert/integrity now require integer nonnegative ticks, finite 20–400 BPM and step/linear curves; meter upsert/integrity require integer nonnegative ticks, integer 1–32 numerators and the exact declared denominator set. Attributed add/update/delete lifecycles preserve creation metadata and restore semantic state on inverse. Exact boundaries pass, chronological indexes remain intact and tick-zero anchors remain present. No equal-tick precedence was asserted. Musical interpretation, snapping, authoring, collaboration, storage, UX, compatibility and release policy remain unchanged and outside this slice.

The isolated QA-01 clip mutable-value contract passes 2/2, and all fifteen exact pure-core files pass 30/30 against the final reducer. Clip add/update/integrity now enforce the declared 1–200-character name, 0–40-character color, boolean mute/loop/reverse flags, linear/equal-power/s-curve fade literals and audio stretch/repitch modes. In-memory MIDI/audio add, attributed update and delete lifecycles restore semantic state on inverse, preserve creation metadata and leave explicit clip order unchanged. Existing timing/source/fade-duration/loop/warp rules and cross-kind behavior remain unchanged. Overlap, snapping, take-lane reassignment, media or asset-length containment, audio split partitioning, musical interpretation, editing UX, storage, compatibility, collaboration and release policy remain outside this slice.

The isolated QA-01 device mutable/parameter contract passes 2/2, and all sixteen exact pure-core files pass 32/32 against the final reducer. Device add/update/integrity now enforce the declared 1–500-character name, boolean bypass/degraded flags, nonnegative integer latency, optional state-asset ID and preset-name shapes. Parameter records require exact key/ID identity, declared name/unit/automatable shapes, finite ordered min/max bounds and current/default values inside those bounds; `device.parameter.set` accepts both exact endpoints and rejects nonfinite or out-of-range values before mutation. Attributed add/update/set/delete lifecycles restore semantic state on inverse, preserve creation metadata and leave device order unchanged. Reciprocal track ownership, state-asset references, move/delete behavior and parameter metadata/default/bounds remain intact. DSP behavior, preset round trips, plug-in discovery/loading, normalization beyond declared bounds, routing cycles, automation ownership, storage, collaboration, UX, compatibility and release policy remain outside this slice.

The isolated QA-01 send/sidechain value contract passes 2/2, and all seventeen exact pure-core files pass 34/34 against the final reducer. Send upsert/integrity now enforce finite `-120..24` gain and boolean pre-fader/enabled flags; sidechain upsert/integrity enforce nonnegative integer bus indices and boolean enabled state. Exact gain endpoints and bus zero pass. Attributed create/update/delete lifecycles preserve creation metadata and restore semantic state on inverse, while existing track/device endpoint checks remain unchanged. No self-route or cyclic-route prohibition was introduced. Graph/DSP behavior, automation ownership, routing UX, storage, collaboration, compatibility and release policy remain outside this slice.

The isolated QA-01 take-lane mutable-value contract passes 2/2, and all eighteen exact pure-core files pass 36/36 against the final reducer. Take-lane add/update/integrity now enforce the declared 1–200-character name and boolean active value. Attributed add/update/delete lifecycles preserve creation metadata, restore semantic state on inverse and retain explicit `clipIds` order; existing reciprocal track/clip membership, comp ranges and add/delete cleanup remain unchanged. No active-lane uniqueness/selection, comp overlap/selection, recording capture, track-deletion retention, lane reassignment, playback, storage, collaboration, UX, compatibility or release policy was introduced.

The isolated QA-01 generation-provenance mutable-value contract passes 2/2, and all nineteen exact pure-core files pass 38/38 against the final reducer. Provenance register/update/integrity now enforce the optional 100-character `modelVersion` ceiling, at most 200 transformation entries and at most 500 characters per entry; empty versions and exact boundaries pass without normalization. Attributed register/update lifecycles preserve creation metadata, primary/reference asset relationships and restore semantic state on inverse. No provenance-delete operation was invented. Provider/model interpretation, cost/currency, rights, retention, deletion/cascade, generation behavior, storage, collaboration, UX, compatibility and release policy remain unchanged and outside this slice.

The isolated QA-01 variant mutable-value contract passes 2/2, and all twenty exact pure-core files pass 40/40 against the final reducer. Variant register/update/integrity now enforce the declared 1–500-character name, active/merged/discarded status literals, nonnegative integer `projectRevision` and optional 1–240-character snapshot asset ID; clearing the optional snapshot remains supported. Syntactic validation precedes lookup, while validly shaped missing base-checkpoint/snapshot references retain their existing fail-closed checks. Attributed register/update lifecycles preserve creation metadata and history relationships and restore semantic state on inverse. No transition matrix or variant-delete operation was invented. Merge/discard meaning, checkpoint deletion/cascade or retention, generation, storage, collaboration, UX, compatibility and release policy remain unchanged and outside this slice.

The isolated QA-01 media-asset metadata contract passes 2/2, and an independent pinned-Node-24 repeat passed all twenty-one exact pure-core files / 42 tests against reducer SHA-256 `90F7B69292CD45A257BFA28B140CBE7D29E6965A0586597A0B28B6BD9EC7395B`. Asset add/integrity enforce the declared kind/name/MIME/SHA-256/byte-length/storage literals and bounds, optional relative/external path lengths, optional sample-rate/channel/duration shapes and optional source literals. Exact boundaries and every enum value pass; both path fields may coexist or be empty, and optional audio-shape fields remain literal metadata rather than inferred relationships. Attributed add/delete lifecycles preserve asset identity and restore semantic state on inverse, while all existing media/history references and deletion guards remain unchanged. Path containment or relative/external coupling, storage backends, decoding, sample relationships, retention, cascade, generation, collaboration, UX, compatibility and release policy remain outside this slice.

The isolated QA-01 checkpoint declared-value contract passes 2/2, and an independent pinned-Node-24 repeat passed all twenty-two exact pure-core files / 44 tests against reducer SHA-256 `EEA03E2EE90BB7D481DBD7554E5BFDCAEC7D28774D5D9D5421476B2BA5696086`. Checkpoint register/integrity enforce the declared 1–500-character name, nonnegative integer project revision, 1–240-character snapshot asset ID, boolean automatic flag and optional 2,000-character reason. Syntactic checks precede snapshot lookup; exact boundaries, both boolean values, empty/omitted reason and minimum/maximum valid snapshot IDs pass. Attributed register/delete lifecycles preserve checkpoint and snapshot identity and restore semantic state on inverse, while existing snapshot references, variant base-checkpoint relationships and deletion guards remain unchanged. No checkpoint update operation, automatic-checkpoint meaning, reason interpretation, deletion/cascade, variant retention, storage, collaboration, UX, compatibility or release policy was introduced.

The isolated QA-01 activity-ledger contract passes 2/2, and an independent pinned-Node-24 repeat passed all twenty-three exact pure-core files / 46 tests against reducer SHA-256 `56FEBF9CD963FDE9834B3BB73AB363E99131C35BBB543B11929A3C5B7ECC2C4F`. Stored integrity enforces 1–240-character entry/actor/optional transaction IDs; human/agent/system actor kind, 1–100-character name, 1–40-character color and declared optional client-field ceilings; 1–500-character labels; all seven declared status literals; UTC ISO timestamps; nonnegative integer revisions; and an optional opaque record for details. Generated commit, undo and redo entries preserve authenticated actor attribution and semantic history. The corrected 2,000 recent-entry runtime window applies consistently to every generated append, while stored projects remain compatible through the schema's separate 10,000-entry ceiling. No meaning was assigned to partial/conflict/failed/checkpoint statuses, details remain uninterpreted, and no ordering, retention, migration, identity, collaboration, UX, storage, compatibility or release policy was added.

The isolated QA-01 project-root metadata contract passes 2/2, and an independent pinned-Node-24 repeat passed all twenty-four exact pure-core files / 48 tests against reducer SHA-256 `3D6CA990829225DC802C7BE2CE5E0084D34F302431FE17C87B45F8F3FD127ECB`. `createProject` and stored integrity share validation for the declared 1–240-character project ID, nonnegative integer revision, UTC ISO creation/update timestamps, created-by actor/client fields, dirty boolean and optional 32,000-character project path. Exact boundaries, empty/omitted paths, both dirty values and independently valid reverse-ordered timestamps pass; creation clones the creator and rejects malformed creators before returning. Authenticated transactions retain the original creator while attributing their activity to the authenticated editor. Existing format/schema/name/kind/settings behavior is unchanged. No timestamp ordering, revision-transition, dirty/save meaning, path containment/storage, actor identity, migration, collaboration, UX, compatibility or release policy was introduced.

The first isolated QA-01 EntityBase contract passes 2/2, and an independent pinned-Node-24 repeat passed all twenty-five exact pure-core files / 50 tests against reducer SHA-256 `3F1FCE35005426B8E9EA187E7B3EAE59413417E8A9C607026DD924F42005764A`. Tempo-event, time-signature-event, marker and section stored records plus their existing add/upsert/update/delete boundaries require declared 1–240-character entity/creator/updater IDs, nonnegative integer revisions and valid UTC ISO creation/update timestamps. Incoming metadata is checked before normalization and malformed stored targets cannot escape through deletion. Existing creation/update attribution, semantic inverses, order arrays and timeline/text/value relationships remain intact. Exact ID boundaries and independently valid reverse-ordered timestamps pass without requiring map-key/entity-ID equality. No timestamp ordering, revision progression, key coupling, actor existence/identity, chronology, overlap, snapping, arrangement, migration, collaboration, UX, storage, compatibility or release policy was introduced.

The isolated top-level track/clip EntityBase contract passes 2/2, and an independent pinned-Node-24 repeat passed all twenty-six exact pure-core files / 52 tests against reducer SHA-256 `FF63B39EAB163F4D90E753E654E7C9CF94896177DA85EACF4A85A3DFF3F86D6A`. Stored tracks and top-level clips plus every existing track/clip add, update, move, trim, split and delete boundary require the same declared EntityBase IDs, revisions and UTC ISO timestamps; split validates the incoming right clip, and cascade track deletion validates each top-level clip before mutation. Exact boundaries and independently valid reverse-ordered timestamps pass while coherent existing membership is retained. Attributed add/update/move/trim/split/delete lifecycles preserve creation metadata, hierarchy/clip order and semantic inverse state. Nested MIDI notes/controls/pitch bends, warp markers, take lanes and comp segments remain outside that slice. No timestamp ordering, revision progression, map-key coupling, actor existence/identity, hierarchy retention, overlap, snapping, lane reassignment, split-source partitioning, migration, collaboration, UX, storage, compatibility or release policy was introduced.

The isolated take-lane/comp-segment EntityBase contract passes 2/2, and an independent pinned-Node-24 repeat passed all twenty-seven exact pure-core files / 54 tests against reducer SHA-256 `21E296821C21FB607D968581C8543D322AE20D52CF18E2F3903E8E1E799A311B`. Stored take lanes and comp segments plus lane add/update/delete and segment create/update/delete upsert boundaries require declared EntityBase IDs, revisions and UTC ISO timestamps. Lane deletion preflights every related comp segment before clearing reciprocal clip membership or deleting anything, so malformed dependent metadata cannot escape cleanup. Exact boundaries and independently valid reverse-ordered timestamps pass while coherent references are retained. Attributed lifecycles preserve creation metadata, explicit clip order, comp ranges, cleanup behavior and semantic inverse state. Track/clip, nested MIDI and warp metadata were not extended by that slice. No timestamp ordering, revision progression, map-key coupling, actor existence/identity, active-lane uniqueness/selection, comp overlap/selection, recording, retention, reassignment, playback, migration, collaboration, UX, storage, compatibility or release policy was introduced.

The isolated nested MIDI EntityBase contract passes 2/2, and an independent pinned-Node-24 repeat passed all twenty-eight exact pure-core files / 56 tests against reducer SHA-256 `B4114BFA9EAC52C751DD461EC558625E8CDB97D6BA3D878EA7AD40245168FEFE`. Stored notes, controls and pitch bends plus their existing direct MIDI add/update/delete boundaries require declared EntityBase IDs, revisions and UTC ISO timestamps; notes selected by the existing semantic operation are checked before transformation. Pre-populated MIDI clip adds validate every nested event before normalization can replace supplied metadata. Exact boundaries and independently valid reverse-ordered timestamps pass while the established map/order identity and insertion order remain intact. Attributed note/control/bend lifecycles and the existing transpose path preserve creation metadata and semantic inverse state. No control-update operation or new transformation was introduced. Warp markers, automation points and other entity families remain outside that slice, as do timestamp/revision ordering, new map-key coupling, actor existence/identity, chronology, containment, note overlap, quantize/humanize, musical interpretation, migration, collaboration, UX, storage, compatibility and release policy.

The isolated audio warp-marker EntityBase contract passes 2/2, and an independent pinned-Node-24 repeat passed all twenty-nine exact pure-core files / 58 tests against reducer SHA-256 `A0FAC287D6F42BD11E15D8E197D1369DDC60197AD138A3CCEE450B86D969D8D9`. Stored markers and every clip boundary that adds, replaces, carries, trims, splits or deletes their audio clip require declared marker/creator/updater IDs, nonnegative integer revisions and UTC ISO timestamps; track-cascade deletion uses the same preflight. Existing markers are checked before an update may replace them, and split validates both the source and supplied right-clip marker arrays. Exact boundaries, duplicate IDs, explicit reverse array order and independently valid reverse-ordered timestamps pass. Attributed add/update/move/trim/split/delete lifecycles preserve marker metadata, source-sample/project-tick values and semantic inverse state. Top-level clip fields, MIDI events, automation points and other families were not extended. No timestamp/revision ordering, map-key coupling, actor existence/identity, warp chronology/uniqueness, clip containment, snapping, audio split source/marker partitioning, transformation, migration, collaboration, UX, storage, compatibility or release policy was introduced.

The isolated automation lane/point EntityBase contract passes 2/2, and an independent pinned-Node-24 repeat passed all thirty exact pure-core files / 60 tests against reducer SHA-256 `323FAB8C45347AA03940A6F06A767EFB331A6D67D6BFCED9FA520760E3C31811`. Stored lanes and points plus lane add/update/delete and point upsert/delete boundaries require declared 1–240-character entity/creator/updater IDs, nonnegative integer revisions and UTC ISO creation/update timestamps. Pre-populated lane points are checked before normalization, existing carried points are checked before mutation or ordering, and track/device cascade cleanup uses the same preflight before deleting automation data. Exact independent boundaries and reverse-ordered valid timestamps pass while current lane membership, explicit lane-add order, point-upsert ordering, targets, tick/value/curve data, attribution and semantic inverses remain intact. No timestamp or revision progression, new map-key/entity-ID coupling, actor existence/identity, cross-track automation, point chronology/uniqueness/containment, curve interpretation, snapping, transformation, migration, collaboration, UX, storage, compatibility or release policy was introduced.

The isolated device/send/sidechain EntityBase contract passes 2/2, and an independent pinned-Node-24 repeat passed all thirty-one exact pure-core files / 62 tests against reducer SHA-256 `C85C4A6A6867C8FE0D3F5259AA200764E93F13412C7BBCD32C176765584E3368`. Stored devices, sends and sidechain routes plus device add/update/move/parameter-set/delete, send upsert/delete and sidechain upsert/delete boundaries require declared 1–240-character entity/creator/updater IDs, nonnegative integer revisions and UTC ISO creation/update timestamps. Existing targets are checked before replacement or mutation; device deletion preflights related sidechains, and track deletion preflights every device/send/sidechain it already removes before any cleanup. Exact independent boundaries and reverse-ordered valid timestamps pass while device order/membership, parameter descriptors, routing endpoints, levels, flags, attribution, cleanup and semantic inverses remain intact. Device parameters were not made EntityBase records. No timestamp or revision progression, new map-key/entity-ID coupling, actor existence/identity, self/cyclic routing, cross-track/device policy, graph validity, endpoint interpretation, parameter semantics, migration, collaboration, UX, storage, compatibility or release policy was introduced.

The isolated `project.settings.update` operation/inverse property contract passes 2/2. An independent pinned-Node-24 repeat passed all thirty-two exact pure-core files / 64 tests against unchanged reducer SHA-256 `C85C4A6A6867C8FE0D3F5259AA200764E93F13412C7BBCD32C176765584E3368` and project-settings test SHA-256 `596DA25AF9C88254C21F467A55582B00E2B1CC7005AE6C6A704F67D47005D087`. A deterministic matrix covers every declared settings field and enum/boundary value: each partial merge changes only named fields, inverse patches restore exact semantic pre-state, and forward patches restore exact semantic committed state. A separate stacked sequence proves two partial updates undo in reverse order and redo in forward order. Existing edit-only crossfade validation and all declared settings shapes remain unchanged; no new default, timestamp, revision, migration, persistence, collaboration, UX, compatibility or release semantics were introduced.

These owner-privacy, save-audit and discovery source bytes are packaged only as immutable subject `test-results/package-prep/20260805T190635Z-post-mcp-owner-audit-discovery/AIMuse-win32-x64/AIMuse.exe`, SHA-256 `9456A999AF1DEA3910A3AA81D42E0CD136C721957038E6E3F9138CDBB69B32BC`, 225,614,336 bytes; ASAR 9,393,729 bytes. The independent run above earned its launch-free and live MCP subset but did not earn packaged UI-approved save acceptance. It inherits no older certificate and must remain `BLOCKED` until a fresh safe PID-bound Computer Use rerun completes the missing cases.

The earlier subscription runtime remains independently Level 1 certified only for isolated package `test-results/package-prep/20260805T063831Z-post-mcp-resource-subscriptions/AIMuse-win32-x64/AIMuse.exe`, SHA-256 `4CF335C1787F91C036F17FE3AA4A2F6309463F231D306A688B86A43633B16EC7`, 225,614,336 bytes; ASAR 9,334,564 bytes. Package-preparation and headless contract evidence for newer source are not substitutes for a complete independent report.

Status, show and stop additionally gate active checks in strict order: executable hash and connection PID/URL/instance/profile must agree before any PID or health probe; a stale PID prevents health access; and stop requires the declared process to remain stable across two AIMuse health-identity probes and three liveness checks before launching the graceful quit signal. Future connection and health payloads carry one non-secret per-process UUID and one SHA-256 profile-path identity, both retained in manifest/redaction evidence. Quit and show are instance-bound at the profile-owner receiver. Older certified payloads remain compatible through required AIMuse name/status/PID validation and their legacy no-instance/no-profile path, but only for the two retained executable hashes; this compatibility does not extend either certificate to changed source.

Coordinator manifest and redaction writes use a same-directory temporary file, fsync, JSON reparse plus identity validation, and one atomic rename. The app-side private connection writer also verifies staged bytes exactly and cleans its temporary file on every pre-commit failure. Test-owned partial-write, silent-truncation, validation-rejection, concurrent-writer and two-file recovery fixtures require the previous target to remain valid, no credential-bearing stage to survive, PID/URL/instance/profile identity to remain coherent, and later status/show/stop to stay fail-closed.

Connection readiness and pre-signal stop races use injected clocks, file reads, process generations, health probes and quit signals. Late readiness succeeds only after one complete local PID/instance/profile-matching connection and health subject appear; malformed input at the deadline makes no health call. Exit, PID recycle, health-instance change and final target change all fail before the signal stand-in and preserve test-owned credential/evidence sentinels byte-for-byte.

Attach/show uses a fully injected coordinator slice: executable hash and connection identity are checked twice around at least four health probes and six same-PID generation inspections. Every helper carries a fresh request UUID plus the exact engine UUID and hashed profile identity. The receiver publishes a bounded, credential-free `pending`, `accepted` or `rejected` acknowledgement on health, bound to that PID/instance/profile, before and after servicing the window request; saturation refuses a new record instead of evicting a pending or recent acknowledgement. Helper exit zero only permits acknowledgement polling; the manifest is updated only after one timely, canonically timestamped accepted acknowledgement remains byte-for-byte equivalent across a final health and process-generation re-probe. Wrong hash/profile/PID/URL/UUID, stale or recycled processes, ambiguous or changing health ownership, helper-owner takeover, launch failure, helper timeout/nonzero exit, missing/pending/rejected/timed-out/stale/duplicate/late/future/internally inconsistent/malformed acknowledgements, post-launch identity change and atomic-writer failure never replace known-good evidence. A bound helper remains forbidden from becoming a new primary owner or creating a window after the verified owner is lost.

After an authorized instance-bound signal, cleanup never signals again. Injected post-signal polling checks only the declared PID/UUID/profile generation, tolerates health unavailability while the verified engine is shutting down, and requires two consecutive not-alive observations plus the redaction guard before replacing private connection evidence. PID/health changes, ambiguous generation or liveness, timeout and redaction failure never redact. Timeout/change keeps the private connection byte-identical and records `connectionCredentialsRedacted: false`; the public manifest/result remains credential-free and exact about `stopOutcome`, stopped state and cleanup status.

AGT-04 has separate real headless evidence at [`20260806T032946Z-bootstrap-restart`](../test-results/agt04-headless/20260806T032946Z-bootstrap-restart/report.md) for exact package hash `9F82CC2C…A758C`. The run root was absent and had Windows ACL inheritance removed before any bearer existed; only the launching user, SYSTEM and Administrators retained access. Two `--headless` cycles on one profile used distinct PIDs/instance UUIDs, returned the exact zero-budget authority/trusted-folder handoff, exposed no exact-PID window handle/title, authenticated then DELETE-closed MCP, and ended `stopped-redacted`. Restart re-read the new private handoff and authenticated after a protected-token rotation. Final scanning found neither live token anywhere below the root, no provider credential file and zero AIMuse processes; the package hash and independent BLOCKED report remained unchanged. This is a headless lifecycle acceptance, not a Luna/high certificate and not provider-credential/Electron-renderer evidence. The earlier `20260806T032557Z-bootstrap-restart` root is retained as a safely stopped/redacted harness-design attempt rejected only because its first version invented token-continuity requiredness.

`npm run verify` also runs `test:renderer:headless`, a Chrome-based renderer control suite with a canonical desktop-API mock. It is safe for non-interactive development sessions and writes only below `test-results/renderer-headless`; it catches inert controls and transaction-wiring regressions but does not replace packaged Electron or Computer Use acceptance.

## Exact-build and engine identity preflight

Formal QA never assumes a registered AIMuse MCP endpoint and visible window belong to the same engine. Multiple builds/profiles can coexist.

1. Run the automated level through `scripts/npm-node24.mjs`.
2. The independent tester records the verified executable path/SHA-256 and planned isolated profile, connection and manifest paths, then returns `AUTOMATION_COMPLETE_AWAITING_COORDINATOR_LAUNCH`. It does not spawn Electron.
3. The primary coordinator checks every artifact path lies under the declared `test-results` run root and invokes `qa-session start` with explicit shell escalation and `--launch-context unsandboxed-gui`.
4. Resume the same tester. It runs `qa-session status` and requires `okay: true`, live PID, matching connection PID/URL and unchanged executable hash.
5. The tester initializes Computer Use, reads its guidance/confirmation rules and enumerates Windows apps without input.
6. It selects the process-backed AIMuse window matching the manifest executable. The Agents/Activity surface must display the same isolated MCP URL.
7. It initializes `scripts/qa-mcp.mjs` from the isolated connection. The globally registered/user-profile MCP is forbidden.
8. Only after executable/hash/PID/URL/window assertions agree may it create a QA project. A mismatch is `BLOCKED` before mutation.

Credential-bearing connection and MCP state files remain below ignored `test-results/`, are never printed or pasted into reports, and are redacted during cleanup.

On Windows, a run-owned credential root must also have inheritance removed and grant access only to the launching user, SYSTEM and Administrators before start. Node's `0o600` flag is not by itself a Windows ACL boundary. `npm run qa:headless-restart -- --exe <absolute exe> --expected-sha256 <hash> --run-root <new child below test-results>` creates and verifies this protected-root contract for the isolated AGT-04 lifecycle. Every general `qa-session` and `qa-mcp` command requires the same existing root through `--private-root`. Start/init persist its non-secret normalized path plus canonical/device/inode identity in coordinator-owned manifest/state; every follow-up revalidates ownership, protected inheritance, allowed principals, contained link-free canonical paths and the same directory object before credential reads/writes, PID/health/window/signal/network calls or evidence output. Missing legacy identity and any root/path drift fail closed; commands never infer or repair an unsafe caller-owned ACL.

`npm run qa:windows-private-root -- --run-root <absolute new direct child of test-results>` isolates the default, non-injected Windows verifier without launching AIMuse. It creates a broad inherited container with protected and inherited sibling subjects, uses the production `whoami`/`Get-Acl`/`icacls` path, requires three identical canonical/device/inode observations for the protected subject and requires the inherited sibling to fail specifically at protected-inheritance validation. Run it as one whole command under the same unsandboxed user that would launch AIMuse; a sandbox principal is not equivalent evidence. PASS retains only credential-free `summary.json` and `report.md` under the exact root. Failure cleanup rechecks the run-root directory object and removes only that root; later disposal of PASS evidence must likewise use the recorded exact identity. Normal Vitest exercises only the harness safety/evidence contract through injected verifier stand-ins.

The first real default-helper run passed under `test-results/20260806T043400Z-qa10-default-private-root`. `summary.json` is 1,615 bytes / SHA-256 `04C00983B4E9F2165D3301B23F558F95431B553AD9205FF6AE07EFCDCC002655`; `report.md` is 1,271 bytes / SHA-256 `1C842F7AD2E709F2E8424F2E46E64833600CC2FA2CC537C7053D9BE00C5E4ECA`. The protected object retained device `3698481203`, inode `22799473114892210` across all three observations; the broad sibling was rejected for inherited rules. This is credential-free Windows ACL/filesystem integration evidence, not application, package or Luna/high certification.

The separate handle-bound design contract is launch-free and provider-injected. Nineteen cases require legacy fail-closed identity, strict handle-derived ACL/object/capability metadata, a non-delete-share root lease, sticky handle/path drift, fresh rotation-safe snapshots, exact allowlisting, pinned absolute-path atomic replacement and unambiguous close. With no provider it stops before sensitive action. The approved in-process Node-API prototype is opt-in, not staged and not wired to either coordinator; thirty-three provider/diagnostic/CWD/authority/discriminator/share/ACL-lifecycle launch-free tests lock its explicit loaders, CMake isolation, documented-Win32-only API policy, no-wiring boundary, version-1-addon rejection, checked wide-digit-to-ASCII conversion, exact addon path, same-parent share-zero staging, fully qualified destination reconstruction from the pinned parent, stream/process-relative rejection, fresh exact-target handle/ID/security verification, explicit run-root authority and sanitized diagnostics. Fresh snapshots share read plus delete but not write; root/parent handles still deny delete sharing, stages remain share-zero and committed-target metadata handles retain their established mask. The historical ten-case explicit-parent matrix stays unchanged. Current null-root calls may receive only complete absolute targets; static coverage forbids the former basename call shape in both production and diagnostics. The retained snapshot-delete-share run progressed beyond its former Win32-32 phase, then failed at the final JS authority oracle because two negative fixtures added an inheritable Users-SID grant that the old `/grant:r` restoration did not remove, and the sticky case mutated the primary accepted root. Current harness source uses separate run-owned negative roots, removes only that exact grant recursively without continue-on-error, re-protects the root, revalidates root plus every affected child and preserves fixed phase-only evidence for any residual unexpected principal.

Preserve all four pre-diagnostic infrastructure FAIL roots, the historical explicit-parent diagnostic PASS, retry4/retry5/retry6, replacement-identity diagnostic, contained-CWD authority failure, explicit-authority snapshot failure, snapshot-phase failure and snapshot-delete-share ACL failure separately; no root may be reused and protected fixtures must not be traversed. The replacement diagnostic is PASS for diagnosis only at `test-results/20260807T041225Z-qa10-node-api-replacement-identity-diagnostics`: all five null-root basenames were resolved below process current directory `E:\AIMuse`, not the protected stage parent, leaving seven fixed owner-private project-root artifacts as untouched incident evidence. The later phase discriminator isolated Win32 32 to `rotation-during-external-replace`; snapshot `FILE_SHARE_DELETE` moved beyond it, and isolated ACL-fixture restoration then removed the final harness defect. Exact retained root `test-results/20260807T060604Z-qa10-node-api-provider-acl-fixture-restoration` is provider-only native PASS: ABI v2, all eight scenarios, two-object revalidation for both negative ACL fixtures, accepted-root isolation, explicit authority and independently contained CWD passed with zero sensitive boundary counters and zero relevant postflight processes. The addon is 469,504 bytes / `5A97F680FBB3695F21C0EB485937F7C93A59477042770DB606097B942FCB107E`; summary/report identities are recorded in [the handle-bound checkpoint](QA10_WINDOWS_HANDLE_BOUNDARY.md). This does not stage or wire the provider and is not a package, product release or application certificate. The [proposal-only integration checkpoint](QA10_COORDINATOR_INTEGRATION_PROPOSAL.md) defines the minimum source/Windows/companion-package matrix; none of its loader, coordinator, staging or signing work is authorized or earned yet.

Example coordinator bootstrap:

```powershell
node scripts/npm-node24.mjs run test:level1:auto
node scripts/qa-session.mjs start --exe E:\AIMuse\out\AIMuse-win32-x64\AIMuse.exe --private-root E:\AIMuse\test-results\luna-high\<run-id>-level1 --profile E:\AIMuse\test-results\luna-high\<run-id>-level1\profile --connection E:\AIMuse\test-results\luna-high\<run-id>-level1\connection.json --manifest E:\AIMuse\test-results\luna-high\<run-id>-level1\session.json --mode interactive --launch-context unsandboxed-gui
node scripts/qa-session.mjs status --private-root E:\AIMuse\test-results\luna-high\<run-id>-level1 --manifest E:\AIMuse\test-results\luna-high\<run-id>-level1\session.json
node scripts/qa-mcp.mjs init --private-root E:\AIMuse\test-results\luna-high\<run-id>-level1 --connection E:\AIMuse\test-results\luna-high\<run-id>-level1\connection.json --state E:\AIMuse\test-results\luna-high\<run-id>-level1\mcp-state.json --actor-name "QA Luna high L1 <run-id>" --actor-color "#7C3AED" --model "gpt-5.6-luna" --effort "high" --task-id "<task-id>"
```

The coordinator owns native `qa-session start`, `show` and `stop`. The tester owns automation, status, MCP, Computer Use, assertions and reporting. Complex MCP arguments go in JSON files below the run root and use `--args-file` so shell quoting cannot change payloads.

## Common run protocol

1. **Identify subject.** Record level, UTC run ID, source revision/dirty state if available, Node/npm, executable path/hash, package verification output, isolated profile and initial active project.
2. **Run automation.** Preserve complete command and exit status. A narrower retry cannot turn a failed level command into Pass.
3. **Pause for launch.** Return the exact automation checkpoint; the coordinator starts the declared process outside the sandbox.
4. **Select native window safely.** Initialize Computer Use and prove executable/window/MCP identity before mutation.
5. **Join isolated MCP.** Use a distinct actor/color/model/effort/task ID; observe before mutation and record project IDs/revisions.
6. **Exercise UI with Computer Use.** Observe, perform one state-derived action, refresh and verify. Re-observe after layout/modal/focus changes; use real pointer input for timeline, piano-roll or mixer gestures.
7. **Cross-check both directions.** Verify an MCP-created change in that exact window and a Computer-Use-created change in MCP snapshot/diff.
8. **Record failures immediately.** Include expected/actual, reproduction, project/revision, window/focus state, visual/log evidence, severity and tracker IDs.
9. **Clean safely.** Restore the initial isolated tab, close/redact MCP state, save only QA projects to new run-root paths and return `TEST_COMPLETE_AWAITING_COORDINATOR_STOP`. The coordinator stops the engine; the tester resumes to prove PID exit/redaction.
10. **Finalize report.** Use [testing/REPORT_TEMPLATE.md](testing/REPORT_TEMPLATE.md). Overall values are only `PASS`, `FAIL` or `BLOCKED`.

Reports belong at:

```text
test-results/luna-high/<UTC-run-id>-level<1|2|3>/report.md
```

## Level 1 — Smoke

Automated command:

```powershell
node scripts/npm-node24.mjs run test:level1:auto
```

Required MCP cases:

- authenticate and `session_manage` join/inspect;
- list projects and record the initially active project;
- create a small uniquely named QA Song or SFX project relevant to the change;
- observe canonical state and attached-editor hints;
- apply one visible idempotent transaction, verify revision/attribution, repeat its client ID and require `duplicate`;
- actor undo and redo;
- close the MCP session cleanly.

Required Computer Use cases:

- prove exactly one intended AIMuse window is healthy and not blank;
- open the New Project flow and cancel without creating a project;
- activate the MCP-created QA project and visibly verify its name/type;
- for Song, create a MIDI clip and use a real pointer change on a track/mixer control; for SFX, create a deliverable and change one bounded parameter;
- exercise human Undo and Redo through native shortcuts;
- verify tabs, transport, browser, arrangement, lower dock, inspector/activity/jobs/agents and engine state remain responsive;
- observe the UI-created mutation through isolated MCP and restore the original project.

Level 1 passes only when automation, MCP, native UI and both cross-surface directions pass.

## Level 2 — Regression

Automated command:

```powershell
node scripts/npm-node24.mjs run test:level2:auto
```

Level 2 includes Level 1 plus:

### MCP and collaboration

- read every public resource and exercise every tool action without paid requests or real recording;
- two sessions, attribution, actor undo, duplicate IDs, stale revisions, human time-range lock conflict, cancellation, checkpoint restore, branch compare/merge/discard and trace replay;
- headless engine → attach → close editor while health stays live → reattach → explicit quit;
- disposable-root read/write approval, denial, timeout, exact overwrite and out-of-root rejection;
- save/reopen recovery and explicit close/discard behavior.

### Computer Use Song workflows

- native File Open/Save As/Close and keyboard equivalents using only new disposable paths;
- add audio/instrument/MIDI/folder/aux tracks and verify hierarchy/routing surfaces;
- arrangement selection, move, trim/split controls, fades/gain/reverse/transpose/stretch metadata where exposed;
- piano-roll notes, velocity/semantic buttons, mixer mute/solo/arm/pan/gain, device insertion, automation dock, markers/sections/lyrics surfaces;
- checkpoint, Activity actor history, Stop agents and reconnect state.

### Computer Use SFX workflows

- layered clips, deliverable selection/range, tags/naming, variants, pitch/timing/tail/LUFS, seamless loop and WAV batch job;
- verify output manifest through MCP/job state without overwriting an existing destination.

### Interchange sample

- native folder save/reopen and `.aimusepack` round trip;
- WAV and MIDI import/export, master/stem/SFX WAV output and one DAWproject export with explicit expected warnings;
- mock/provider-validation candidate accept/reject with no paid call.

No Blocker/P0 finding and no unexplained mandatory skip is allowed.

## Level 3 — Release exhaustive

Automated command:

```powershell
node scripts/npm-node24.mjs run test:level3:auto
```

Level 3 includes Levels 1 and 2 plus the complete selected-v1 acceptance:

### Build and clean-machine evidence

- committed reviewable baseline, clean dependency install and exact Node 24 environment;
- installer and portable artifacts on clean supported Windows 11 x64;
- install, launch, headless/attach, engine quit, uninstall and portable behavior;
- SHA-256, license/SBOM, dependency audit, native SDK license record and signing/unsigned policy.

### Exhaustive MCP and authority

- all resources/subscriptions/notifications, all tool actions, annotations and malformed/boundary payloads;
- 32 sessions, transaction limits, idempotency, locks/retries, actor history, jobs/cancellation, checkpoints/merge, authority exhaustion and long headless sequence;
- canonical/reparse/case path tests, exact overwrite, plug-in and recording approvals;
- server attribution, provenance and referential-integrity attacks.

### Exhaustive native UI

- every visible menu, shortcut, dialog, toolbar, panel tab, inspector control, timeline/piano-roll/mixer/automation/SFX control and empty/error/loading/approval state;
- minimum/typical/large window sizes, 100%/200% display scaling, keyboard-only navigation, high contrast/reduced motion, focus restoration and readable non-color-only actor labels;
- complete Song and SFX creation/edit/save/reopen/checkpoint/export/recovery scenarios with every persisted UI mutation cross-checked via MCP or reopen.

### Audio, plug-ins, interchange and providers

- AUD-02 preview shutdown has two deliberately launch-free contracts. `tests/main/audio-preview-lifecycle.node.mjs` passes 2/2 and proves all owned refresh/build work settles before teardown. `tests/main/audio-child-lifecycle.node.mjs` passes 4/4 and proves an acknowledged shutdown waits for the exact child's natural exit, the fallback is unavailable before the 1,500 ms grace deadline, a lost response still permits natural exit, listeners are removed, and the controller source awaits that policy. Both exact commands use pinned Node's strip-types/test runner and import only their pure helpers plus fixed Node built-ins; neither imports child-process, native, audio/device, Vitest, app or package runtime code. Typecheck and exact-file lint also pass.
- The dedicated [AUD-02 native teardown checkpoint](AUD02_NATIVE_TEARDOWN_CHECKPOINT.md) retains all attempts separately. Retry 2 is immutable conclusive FAIL evidence for the premature fallback race. Corrected retry 3 passed at immutable `test-results/20260807T072744Z-aud02-native-teardown-retry3`: exact identities, 48 kHz WASAPI shared output, bounded preview, pending refresh, natural PID exit code 0/no signal without fallback, retained-cache rename plus 2,000 ms quiet-window stability, unchanged endpoint, zero relevant/unexpected entries and no cleanup/sensitive authority all passed. This closes the prior `ENOTEMPTY` and premature-fallback classes only for the exact frozen source/helper/controller/audio binary and scenario; package/app/UI/scanner, exclusive mode, hot-plug and wider audio acceptance remain unearned.
- The bounded exclusive-mode source contract keeps `--stdio` as the unchanged shared-mode launch, adds only explicit `--playback-mode=exclusive`, maps that request directly to miniaudio shared/exclusive configuration with no fallback retry, and reports `requestedPlaybackMode` plus `effectivePlaybackMode` in hello and transport state. The exact launch-free pair passes 2 files / 5 tests and is independently accepted. Its [two-phase native checkpoint](AUD02_EXCLUSIVE_NATIVE_CHECKPOINT.md) then completed PASS under retained root `test-results/20260807T194500Z-aud02-exclusive-native-acceptance`: plan `3F3F85F8…55E6C7`, build summary `BE465730…EA547`, exact audio `651BF50F…4FD1A7` (1,275,904 bytes), native CTest 1/1, then one unchanged default endpoint with matching shared and effective-exclusive hello/transport/playback, two fixed 180 ms/48 kHz/peak-0.02 probes, natural code-0/no-signal exits, zero fallback/force/survivor/unexpected entries and no cleanup. Device summary is `558B04F0…1211E`; case evidence is `D3E3600F…B451F`. This is exact executable/endpoint/scenario acceptance, not contention/rejection, hot-plug, package/app/UI, release or macOS evidence.
- macOS verification is intentionally separate. `npm run verify:macos` is defined for a real Darwin host and covers the allowlisted portability check, explicit-root TypeScript/ESLint, headless source contracts and CMake DSP/playback/parser tests with runtime binaries disabled. It has not run on macOS. CoreAudio, Keychain, native lifecycle/device, Electron UI, signing, entitlements, notarization and package verification remain unearned.
- WASAPI shared/exclusive, audio/MIDI recording, monitoring, punch/loop/takes, latency/PDC, real-time/offline match and recovery;
- native DSP golden/null corpus and synthetic VST3/CLAP success/malformed/crash/hang/latency/state/missing/sidechain/automation matrix;
- maintained WAV/FLAC/MP3/AAC/OGG/MIDI/DAWproject/ZIP64 fixtures, explicit losses and >4 GiB pack;
- mocked provider success/moderation/rate/timeout/cancel/capability drift/cost ambiguity with proof of no retry/substitution; no paid network calls.

### Resilience and performance

- corrupted/truncated/oversized media/project/archive, ZIP bomb, traversal, interrupted save/recording, native/bridge/renderer crash and restart;
- pinned Windows reference machine: specified 48 kHz/256-sample callback load below 70% with no xruns for ten minutes, interactive 200-track/10,000-clip project, and eight-hour/20,000-transaction autonomous run with no lost commits.

Level 3 requires all automation and mandatory Computer Use/MCP evidence, no open Blocker/P0, explicitly accepted P1 exceptions linked to tracker IDs, and exact agreement among artifacts, report, README, tracker and known limitations.

## Severity and reruns

| Severity | Meaning | Gate effect |
| --- | --- | --- |
| Blocker | Cannot identify/launch/attach/test, persistent blank screen, data loss/corruption, unsafe authority bypass or untrustworthy evidence | Fails every level immediately. |
| P0 | Core workflow, timing, security, recovery, coordinate or artifact failure | Fails every formal level. |
| P1 | Major advertised workflow broken without safe practical workaround | Fails Level 2/3; may fail Level 1 when changed scope. |
| P2 | Bounded defect with clear workaround | Record; release needs explicit disposition. |
| P3 | Cosmetic/polish | Record and triage. |

After a fix, use a **fresh** Luna/high task. A targeted rerun includes the failed case and Level 1 core; a release candidate always receives a fresh complete Level 3.

Use [testing/LUNA_HIGH_PROMPT.md](testing/LUNA_HIGH_PROMPT.md) as the canonical independent tester prompt.
