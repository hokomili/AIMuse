# AIMuse feature tracker

Last audited: 2026-09-04

This is the source of truth for implementation status. AIMuse is a native agent-driven DAW, not a built-in generative-content platform. External agents edit through authenticated local MCP and may import media created under their own authority; AIMuse ships no provider adapter, provider credential store, generation job/tool/UI, or persistent protected-secret backend.

## Status rules

| Status | Meaning |
| --- | --- |
| ✅ Verified | The workflow is implemented and covered by automated acceptance or contract evidence. |
| 🟢 Working | The workflow is usable now, but acceptance breadth is incomplete. |
| 🟡 Partial | A useful subset works, but material selected-scope work remains. |
| 🟠 Scaffolded | A boundary exists, but the workflow is too thin to call usable. |
| ⬜ Missing | No meaningful implementation exists yet. |
| ⏸ Deferred | Explicitly outside the selected v1 scope. |
| 🗑 Removed | Superseded product surface deleted from the current product and release gates. |

Priorities are `P0` release blocker, `P1` core v1 workflow, `P2` completeness/fidelity, and `P3` polish. Removed rows have no priority and cannot block release. A feature may move to **Verified** only when its current exit criterion has automated evidence.

## Current release truth

- AIMuse remains `0.1.0-alpha.0`, not a completed DAW release or release candidate.
- Strong areas remain the project/reducer model, journal/trace/recovery, actor-safe collaboration, twelve-tool authenticated MCP, deterministic MIDI/media/export/interchange, managed-preview native audio, and hardened Electron shell.
- Major gaps remain physical recording/MIDI, live graph/PDC/stretch/DSP completeness, SDK-backed VST3/CLAP hosting, codecs/interchange fidelity, accessibility, clean-machine packaging, performance/soak, and Level 3.
- MCP clients use one static no-secret stdio setup. A user-private atomic run-state lets the AIMuse-owned bridge discover and authenticate fresh PID/instance-bound engine authority internally across GUI or headless restarts. AIMuse does not write client configuration or use Keychain, DPAPI, libsecret, Electron `safeStorage`, or another protected-secret backend.
- Historical exact-subject Level 1/2 evidence remains history for those bytes only. This checkpoint adds fresh local clean-install, ad-hoc arm64 package, relocated exact-bridge and real stdio-client headless evidence. A later fresh arm64 package closes the observed macOS presentation defect: static `LSUIElement` bootstrap plus serialized activation fallback kept the exact `--headless` PID `BackgroundOnly` and non-frontmost through an ordinary LaunchServices reopen, admitted one editor only through instance/profile-bound show, returned to silent background operation after detach, reattached, and left no job-owned app/helper/native process after terminal quit. Ordinary interactive launch still produced one foreground editor. This does not add playback/recording quality, physical MIDI, non-macOS, Developer ID, notarization, updater, distribution, or Level 3 claims.
- The pending AUD-03 native candidate is preserved as its own bounded work item and was not judged by this product-surface removal.

### Literal release-candidate bar and ranked remaining gaps (2026-08-20)

A release candidate requires one immutable checksummed subject plus fresh independent Level 3 evidence for clean install/portable lifecycle, exhaustive MCP/Computer Use, native audio/recording/MIDI/PDC/DSP/stretch, SDK plug-ins, codecs/interchange, corruption/recovery/security/performance/accessibility, signing/SBOM/licenses/advisories, and exact report/manifest/tracker agreement. Removed provider/generation/protected-storage rows are not part of that matrix and may not be revived as implicit blockers.

### Audit snapshot

The counts below are regenerated during tracker audits; feature work must update both the relevant row and its evidence.

| Measure | Count |
| --- | ---: |
| Tracked items | 116 |
| Verified | 36 |
| Working | 22 |
| Partial | 31 |
| Scaffolded | 6 |
| Missing | 10 |
| Deferred | 0 |
| Removed | 11 |

## 1. Foundation, canonical engine, and storage

| ID | Feature | Status | Priority | Current truth and exit criterion |
| --- | --- | --- | --- | --- |
| FND-01 | Node 24 npm workspace and Electron/React/TypeScript shell | 🟢 Working | P0 | Workspace, Forge/Vite and Node-24 discovery/preflight exist; Electron is exactly pinned to registry-current supported `43.4.0`. Verify clean-clone install/package/launch in hosted Windows CI. |
| FND-02 | Sandboxed renderer and narrow IPC boundary | ✅ Verified | P0 | Context isolation, sandbox, CSP and hardened fuses are enforced. A shared parsed-URL policy now rejects development-origin prefix confusion, credentials, wrong host/port/scheme and malformed URLs; automated negative tests cover exact IPC sender/frame/origin trust, navigation, new windows, webviews and permission denial. |
| FND-03 | Application-owned schemas, migrations, reducer and inverses | ✅ Verified | P0 | Strict schemas, migration fixtures, inverse operations, revisions and integrity failures are covered by core tests. Expand to property tests for every operation family under QA-01. |
| FND-04 | Canonical editor-independent `EngineRuntime` | ✅ Verified | P0 | The exact packaged cross-surface test proves UI and authenticated MCP mutate one project service; headless-only long-run proof remains under QA-06. |
| FND-05 | Single-instance attach/show/quit lifecycle | ✅ Verified | P0 | Packaged Windows QA launches one profile, verifies PID/URL identity and exits via `--quit-engine`. A fresh Darwin arm64 development package now declares `LSUIElement`, starts the exact headless PID as `BackgroundOnly`/non-frontmost with no added app-switcher surface, remains so after an ordinary non-show LaunchServices reopen, promotes to one Computer Use-visible editor only after an acknowledged instance/profile-bound show, removes its renderer and returns to `BackgroundOnly` after close, reattaches on a second bound show, and ends terminal quit with the connection redacted and zero job-owned app/helper/native survivors. A separate ordinary interactive launch still opened one foreground editor and shut down cleanly. Repeat the exact lifecycle for every final package and shipping platform. |
| FND-06 | Incremental `.aimuse` folder persistence | ✅ Verified | P0 | Atomic JSON, content-addressed embedded media, hash checks, warnings and round-trip are tested. Successful saves atomically retain destination-free actor/project/time/outcome `file.saved` audit without revision, reducer activity or undo changes; agent/human round-trip coverage passes. Authority-approved open, save and compound unpack cancellation are terminal. Waiting open/unpack has zero source/destination/workspace/controller effect; returned open settlement remains identity-checked, while unpack independently reports sequential archive output, validation and later open effects without archive-wide atomicity or cleanup promises. Save retains its distinct destination/project/audit effects. Add recording-file interruption fixtures. |
| FND-07 | ZIP64 `.aimusepack` persistence hardening | ✅ Verified | P0 | Pack/unpack, no-overwrite destination, traversal, entry/size/ratio budgets and validation are tested. Unpack progress is non-interfering and source-literal per entry; archive-open and extraction failures enter best-effort destination removal, while failed removal is reported as possibly partial rather than hidden. Execute a real >4 GiB fixture before release. |
| FND-08 | Recovery journal and compaction | ✅ Verified | P0 | Fsync append, replay, malformed-tail handling, compaction ordering and fail-closed removal paths have automated coverage. Formal Level 2 `20260810T061608Z-macos-level2` proved both force-discarded dirty IDs absent after same-profile restart while a separate unclosed dirty control recovered with identical ID, revision, canonical state and history. |
| FND-09 | Durable attributed transaction trace and replay | ✅ Verified | P0 | Trace mirroring, recovery authority and injected mirror failure after durable commit are covered. The public authenticated `trace_replay` MCP tool selects a transaction from the durable trace, emits bounded non-mutating visualization progress and returns a deterministic receipt binding source transaction/operation hashes to canonical before/after hashes; unit, MCP contract/integration and manifest-bound packaged cross-surface coverage pass without hidden preload injection. Fresh independent Luna/high Level 2 `20260810T041234Z-macos-level2` exercised the public tool against a durable transaction, proved zero applied operations and identical canonical before/after state, and retained stable entry/transaction/audit digests. |
| FND-10 | Serialized concurrent mutations and two-phase graph commit | ✅ Verified | P0 | Concurrent UI/agent transactions cannot lose updates; native prepare, journal failure abort, graph commit and mirror-warning paths are fault-tested. |
| FND-11 | Versioned format migrations beyond schema 1 | 🟡 Partial | P1 | Legacy/default upgrades to schema 1 are covered. A future schema migration corpus and unsupported-newer-version fixtures are still required. |
| FND-12 | Feature tracker and release-metadata enforcement | ✅ Verified | P0 | Automated metadata tests require workspace version parity, unique tracker IDs and no stable version while open selected-v1 P0/P1 gates exist. |

Evidence: [core model](../packages/core/src/model.ts), [operations](../packages/core/src/operations.ts), [reducer](../packages/core/src/reducer.ts), [project service](../src/main/project-service.ts), [persistence](../src/main/persistence.ts), [renderer security policy](../src/main/renderer-security.ts), [renderer security tests](../tests/main/renderer-security.test.ts), [journal tests](../tests/main/journal.test.ts), [release metadata test](../tests/main/release-metadata.test.ts).

## 2. Agent-native MCP, authority, and collaboration

| ID | Feature | Status | Priority | Current truth and exit criterion |
| --- | --- | --- | --- | --- |
| AGT-01 | Authenticated stateful localhost MCP | ✅ Verified | P0 | Bearer auth on every TCP path including health and malformed/oversize rejection remain contract-tested. Empty/malformed internal or offered authority is categorically invalid, and a blocked-stop matrix rejects absent, blank, malformed, wrong and stale bearers across health, MCP `OPTIONS`/`GET`/`POST`/`DELETE`, and unknown routes while pending cleanup keeps the listener open. Admitted requests are generation-bound; slow chunked POSTs for an existing session, a new initialize and malformed input all reauthenticate after body completion and fail identity-free when stop intervenes, while restart accepts only fresh authority. The 32-client cap synchronously counts committed plus pending initialization, with genuinely parallel 33-client rejection, failed/protocol-rejected/client-aborted cleanup, pre/post-connect stop races, repeated stop, and post-DELETE recovery proving reservations and unadopted sessions cannot leak. No unauthenticated health or MCP endpoint remains. |
| AGT-02 | Public resources and subscriptions | ✅ Verified | P1 | All four concrete resources, six templates and public read shapes are contract-tested. `aimuse://sessions` filters jobs to its reader and `aimuse://jobs/{id}` is owner-only; foreign and missing IDs share the same error. Exact-string subscription routing/cleanup remains covered headlessly, while the older packaged proof remains scoped to `4CF335C1…B16EC7`. |
| AGT-03 | Twelve tool-first public contracts | ✅ Verified | P0 | Exact names, strict/action-specific schemas, described output envelope, invalid combinations, initialize instructions, `aimuse_help`, optional guide fallback and cold tools-only workflow/privacy discovery are contract-tested. Exhaustive semantics for every domain action remain QA-04. |
| AGT-04 | Stable client bridge and engine-scoped authority | ✅ Verified | P0 | One static no-secret stdio setup starts before a headless engine, connects after launch, survives stop/restart through automatic internal reinitialization, and remains byte-unchanged. The setup passes only the stable target profile. The entry prohibits macOS activation before validation, derives a real link-free Electron sibling (owner-private on POSIX), rejects POSIX links plus injected Windows case/reparse/junction and filesystem-ID aliases, revalidates identity around `app.setPath`, and exits malformed entries without engine-profile fallback. Same-session notification EOF/error restarts exactly once on the next message; close deletes a deferred late-created session. Each engine rotates bearer/instance; old bearer/session use is rejected. Private atomic run-state is PID/instance/profile-bound, POSIX owner-only or Windows protected-ACL, exact-match removed on stop, dead-PID residue replaceable, and live-PID ambiguity fail-closed. A relocated ad-hoc arm64 package passed exact-bridge alias/process checks plus static-client long-idle, simultaneous-client, force/graceful restart and zero-survivor evidence; visual GUI and Windows breadth remain AGT-17. The QA-only export is not onboarding. |
| AGT-05 | Presence, actor cursor/range and activity UI | 🟢 Working | P1 | Presence, client/model metadata, cursor/range and activity render. Add two-session live cursor/range Computer Use evidence. |
| AGT-06 | Human-priority entity/time-range locks | ✅ Verified | P0 | Coarse conflicts, unrelated edits, expiry/release and retry guidance are service-tested. Two authenticated persistent MCP sessions prove exact entity and overlapping time-range `project_apply` conflicts return the bounded retryable lock response without mutation while unrelated edits remain eligible. Fresh independent Level 2 `20260810T041234Z-macos-level2` also passed the real Computer Use drag → visible nonzero grace countdown → MCP `locked` → bounded cleanup → ordinary stale-conflict sequence. |
| AGT-07 | Per-actor undo/redo without stomping later edits | ✅ Verified | P0 | Selective patch matching and overlapping human/agent histories are regression-tested. |
| AGT-08 | Idempotency and expected entity revisions | ✅ Verified | P0 | Duplicate client IDs, stale revisions and concurrent commit order are covered. |
| AGT-09 | Automatic broad-change checkpoints | ✅ Verified | P0 | Broad agent transactions create immutable checkpoints. A valid historical fixture with divergent checkpoint/current provenance proves restore and its undo/redo keep the current passive provenance map and generation-source asset subset structurally and serialization-byte unchanged. |
| AGT-10 | Named variants, compare, merge and rejection | 🟡 Partial | P1 | Create/compare/merge/discard works on stored snapshots. Source-only reducer acceptance now enforces declared mutable name/status/revision/snapshot-ID values with attributed reversible register/update while preserving checkpoint/snapshot references. Status-transition, deletion/retention, exhaustive three-way musical conflict and selective entity merge rules remain undecided or incomplete. |
| AGT-11 | Server-owned attribution and passive provenance compatibility | ✅ Verified | P0 | Authenticated actors override untrusted metadata; generic MCP cannot forge assets, checkpoints, variants, or historical provenance. Direct, branch and authenticated MCP operations reject provenance create/update/delete and generated-source creation; whole-project restore/history/merge installation preserves the current passive provenance and generation-source assets. Existing files remain readable without turning compatibility data into a live mutation route. |
| AGT-12 | Process-lifetime authority policy | ✅ Verified | P0 | Expiry/runtime, canonical roots, exact overwrite, plug-in, microphone, and MIDI decisions are unit-tested. Provider/model and spending/generation authority domains have been removed. |
| AGT-13 | Approval jobs and human boundary | 🟢 Working | P0 | Waiting jobs and once/session/always/deny UI exist. One global exact-job slot is reserved before approval-capable preparation; a concurrent MCP request performs no I/O, creates no job and returns only the fixed sanitized retryable `approval_pending`, while the central store rejects a second waiting publication. Formal Level 2 `20260810T061608Z-macos-level2` proved maximum waiting count one, collision privacy/no write/no job, Computer Use allow and deny, cancellation slot release, and strictly serial stems/MIDI/DAWproject/master/pack/SFX completion. Packaged expiration, session-grant and restart breadth remain. |
| AGT-14 | Owner-scoped jobs/results/approval detail | ✅ Verified | P1 | Two authenticated loopback actors prove `job_manage`, `aimuse://sessions`, and `aimuse://jobs/{id}` cannot reveal or mutate a foreign job. The owner can inspect/poll/cancel and read its human-only approval dependency; foreign IDs equal missing IDs and successful save audit exposes no destination. |
| AGT-15 | Stop agents and partial activity | 🟢 Working | P0 | The editor cancels cancellable agent jobs and clears presence. Injected retained render/file/plugin producers report cooperative partial effects without rollback. Add packaged Stop Agents render and actor-undo evidence. |
| AGT-16 | Auditory agent observation loop | 🟡 Partial | P1 | Audition WAV resources and basic analysis jobs work; authenticated audition/consolidation job creation now has fair admission plus an injected-render cancellation/late-effect contract, and an exact packaged run proved a real non-silent 32-bit-float observation is visible and playable through the stream-enabled engine-owned media route. Waveform/spectrogram images, key/transient analysis and robust before/after comparison remain. |
| AGT-17 | First-class MCP client onboarding | 🟢 Working | P1 | The Agents dialog displays one-time stdio snippets for Codex, Claude Code, OpenCode, Antigravity, and generic clients. Snippets contain only the AIMuse launcher plus stable target profile—no Chromium profile switch, bearer, URL, PID, instance, account credential or per-launch value—and AIMuse never locates or writes client configuration. Source tests bind preparation/revalidation, POSIX and injected-Windows alias refusal, pre-validation prohibited macOS activation, explicit malformed-entry exit, start-before-engine/restart reconnection, same-session SSE recovery and close-race cleanup. A real command-line MCP harness used one byte-stable static config against a relocated exact arm64 package bridge across simultaneous clients, 65-second idle, process death and two ordinary restarts. Add installed named-client-version, visual no-Dock/UI, interactive GUI and non-macOS acceptance. |
| AGT-18 | Fair multi-agent mutation scheduling and backpressure | 🟡 Partial | P0 | Direct/branch edits, history, checkpoints/variants, plug-in device edits, WAV analysis, and audition/consolidation job creation share four fair lanes with actor-round-robin queued service, a four-item per-actor queue, 2,048 global cost, explicit busy results, and queued cancellation. Observation calls bypass admission; producer-specific running partial effects remain owner-scoped. Generation/provider admission families and their mixed-soak fixture were removed. Retained family-specific and approved file/plugin tests remain launch-free. |

Evidence: [MCP host](../src/main/mcp-host.ts), [ephemeral authority](../src/main/mcp-ephemeral-authority.ts), [private run-state](../src/main/mcp-runtime-state.ts), [isolated bridge entry](../src/main/mcp-bridge-entry.ts), [stdio bridge](../src/main/mcp-stdio-bridge.ts), [media manager](../src/main/media-manager.ts), [client profiles](../src/common/agent-clients.ts), [fair scheduler](../src/main/agent-mutation-scheduler.ts), [authority](../src/main/authority-manager.ts), [bridge entry/setup tests](../tests/main/agent-client-bridge-setup.test.ts), [bridge lifecycle/race tests](../tests/main/mcp-stdio-bridge.test.ts), [run-state tests](../tests/main/mcp-runtime-state.test.ts), [ephemeral-authority tests](../tests/main/mcp-ephemeral-authority.test.ts), [retained MCP admission tests](../tests/main), [project-service tests](../tests/main/project-service.test.ts), [MCP contract tests](../tests/main/mcp-host.test.ts), [MCP guide](MCP.md), [parity audit](PARITY_AUDIT.md).

## 3. Song workspace and creator editing

| ID | Feature | Status | Priority | Current truth and exit criterion |
| --- | --- | --- | --- | --- |
| SONG-01 | Song project template and document tabs | 🟢 Working | P1 | Song creation, accessible tabs, dirty/activity state and core settings work. Current `4CF335C1…B16EC7` restored original `New Song` unchanged and retained two isolated run-owned QA Songs; `58E232A3…ED476` likewise retained its QA Song, `CFCC…30621` omitted Discard, and historical `E558…EC412` authorized Discard only for its disposable project. Reopen/recovery matrices remain. |
| SONG-02 | Arrangement timeline and track hierarchy | 🟡 Partial | P1 | Tracks, clips, markers and attributed ranges render with selection, working fold state, pointer move/split tools and instrument-lane MIDI drawing. The Inspector exposes separately named **Clip start tick**, **Clip length ticks** and **Split at midpoint** actions; fresh macOS Level 2 passed native Computer Use for move/trim/split plus marker/section/lyrics with MCP observation. The ruler supports captured continuous drag-to-seek at exact unsnapped ticks with coalesced transport calls. Complete scalable waveform drawing, folder/aux hierarchy gestures and keyboard editing. |
| SONG-03 | Non-destructive audio clip editing | 🟡 Partial | P1 | Model/reducer support move/trim/split, gain, fades, reverse, transpose and stretch metadata. Declared clip text/color, boolean, fade-curve and audio stretch-mode values plus attributed inverse lifecycles pass source-only acceptance. Complete interactive slip/crossfade/consolidate/freeze/bounce workflows and rendered fidelity. |
| SONG-04 | MIDI clip and piano-roll editing | 🟢 Working | P1 | Notes/CC/pitch-bend model, MIDI clip creation, piano-roll dock and semantic operations exist. Complete pointer note/CC/pitch editing and packaged keyboard acceptance. |
| SONG-05 | Quantize, groove, humanize, transpose, legato and arpeggiate | ✅ Verified | P1 | Deterministic semantic MIDI transformations and inverses have reducer coverage. Add groove-template persistence and full editor exposure. |
| SONG-06 | Tempo, meter, markers and song sections | 🟡 Partial | P1 | Canonical maps/sections and musical-time conversion exist; tempo/meter declared values, safe marker and section ranges, marker/section text values, chronological or explicit ordering and attributed inverse lifecycles pass reducer integrity. Marker and section add/update/delete now have separate strict-schema safe-tick rejection without changing equal-tick, overlapping-section or explicit-order behavior. The native **Song** tab creates, edits and deletes markers and sections at the selected range with explicit named/tick/range/energy fields, and renderer integration proves the attributed operations. Fresh Computer Use remains required. Equal-tick precedence, tempo curves, section dragging and tempo-aware native render/transport integration remain incomplete. |
| SONG-07 | Lyrics and arrangement guidance | 🟢 Working | P2 | The canonical lyrics field has fail-closed declared-length integrity and reversible exact-text edits. The native **Song** tab now provides a visible bounded textarea, character count and explicit **Save lyrics** transaction with renderer integration. Fresh Computer Use is still required; synchronized lyric-to-transport display and richer section-prompt authoring remain incomplete. |
| SONG-08 | Take lanes and comp selection | 🟠 Scaffolded | P1 | Take-lane/comp entities and operations exist; source-only acceptance now enforces declared lane name/active values with attributed reversible add/update/delete while preserving reciprocal ordered membership. Active-lane uniqueness/selection, comp overlap/selection, recording capture, lane UI and audible comp playback remain absent or undecided. |
| SONG-09 | Mixer, buses, sends and sidechains | 🟡 Partial | P1 | Mixer UI and routing/send/sidechain model operations exist; source-only acceptance now enforces declared send gain/flags and sidechain bus-index/flag values with attributed reversible upserts. Pan/gain use gesture-end commits and Insert routes to the device browser. Self/cyclic route policy, real-time graph routing, meters, insert DSP and packaged automation remain. |
| SONG-10 | Track/device automation editor | 🟠 Scaffolded | P1 | Lanes, points and target schemas exist with a lower dock, and the empty state now creates a valid volume lane. Drawing, range transforms, write modes and sample-accurate playback are absent. |
| SONG-11 | Freeze, bounce, consolidate and stem render | 🟡 Partial | P1 | Offline render/consolidate and stem jobs exist. Authenticated audition/consolidation job creation now uses fair cost-one admission, while injected-render cancellation evidence makes the running boundary explicit: managed cache plus an already-started asset commit/source registration and consolidation clip commit may remain, and the terminal owner job reports which boundary was reached. This is cooperative cancellation, not render preemption or request-wide rollback. Device-aware freeze/bounce, latency compensation and complete fidelity are absent. |
| SONG-12 | Loudness-targeted master workflow | 🟡 Partial | P1 | Project LUFS target, WAV master render and basic limiter/analyzer device surfaces exist. True loudness analysis, processing and compliant master verification remain. |

Evidence: [editor](../src/renderer/App.tsx), [timeline](../src/renderer/Timeline.tsx), [lower dock](../src/renderer/BottomDock.tsx), [core reducer tests](../tests/core/reducer.test.ts).

## 4. SFX workspace

| ID | Feature | Status | Priority | Current truth and exit criterion |
| --- | --- | --- | --- | --- |
| SFX-01 | SFX project template and layer workspace | 🟢 Working | P1 | SFX creation, layer track, browser and deliverable cards work. Add complete packaged one-shot and loop authoring scenarios. |
| SFX-02 | Named deliverables, tags and naming templates | ✅ Verified | P1 | Strict model/reducer/migration and batch-manifest output are tested. |
| SFX-03 | Deterministic randomized variants | ✅ Verified | P1 | Seeded pitch/gain/timing variation produces distinct deterministic WAVs with bounded controls. |
| SFX-04 | Seamless loop and tail editing | 🟡 Partial | P1 | Batch renderer crops/crossfades loops and handles tails. Interactive loop-boundary audition and zero-click verification remain. |
| SFX-05 | Transient trim, envelopes, reverse and pitch | 🟡 Partial | P1 | Shared audio-clip metadata supports several transforms. Production transient detection, envelope UI and rendered processing remain. |
| SFX-06 | LUFS normalization and batch export | ✅ Verified | P1 | Target normalization, collision-safe naming, WAV variants and metadata manifest are tested. FLAC/MP3 output remains tracked under IO-04. |
| SFX-07 | Game-ready loop points and metadata interchange | 🟡 Partial | P2 | Deliverable metadata records range/tags/template. WAV cue/smpl metadata and external engine presets remain. |

Evidence: [SFX UI](../src/renderer/Sidebars.tsx), [export manager](../src/main/export-manager.ts), [export tests](../tests/main/export-manager.test.ts).

## 5. Native audio, MIDI, recording, DSP, and instruments

| ID | Feature | Status | Priority | Current truth and exit criterion |
| --- | --- | --- | --- | --- |
| AUD-01 | C++20 audio service and stable protocol | ✅ Verified | P0 | Packaged handshake, transport and prepared/committed graph revision protocol are tested. |
| AUD-02 | WASAPI shared/exclusive and CoreAudio shared playback | 🟡 Partial | P0 | The pinned miniaudio revision drives WASAPI shared float32 output. Revision previews render in a worker, prewarm after commits, remain cached, and swap without resetting native cursor/play state; transport, edit-while-playing continuity, callback goldens and host/native protocol contracts pass. Native transport state reports the most recent service-callback load ratio and cumulative strictly-late callback count; the host accepts the pair together or fails closed, while legacy omission retains its last coherent observation. These are service-thread budget observations, not hardware-underrun claims. Retained retry 3 is exact native acceptance for the corrected shared-output teardown/cache scenario and closes the prior `ENOTEMPTY` and premature-fallback race classes for that subject. Current Windows source preserves shared by default and adds only explicit `--playback-mode=exclusive`: miniaudio receives exactly the requested share mode, never retries in shared mode, and hello/transport plus host status expose validated requested/effective mode or precise unavailability. The exact launch-free pair passes 2 files / 5 tests. The independently audited two-phase checkpoint at `test-results/20260807T194500Z-aud02-exclusive-native-acceptance` then built exact audio SHA-256 `651BF50F…4FD1A7`, passed native CTest 1/1 and passed one endpoint run: shared PID 15528 and exclusive PID 10268 each played the fixed probe, reported matching requested/effective WASAPI modes, exited naturally and left no survivor/fallback/force. Darwin arm64 separately passed a real 48 kHz/256-frame CoreAudio five-second soak with 939 callbacks/240,384 rendered frames, stop→play restart, fresh-process restart, natural exits and explicit exclusive rejection without shared fallback. Injected notification transitions fail closed on interruption/unexpected stop; no physical hot-plug/interruption event was observed, so hardware evidence remains external. Endpoint selection, contention, hardware meters/underruns, long soak and real-time/offline graph parity remain; package/UI/release acceptance is separate. |
| AUD-03 | Physical MIDI input/output | 🟠 Scaffolded | P0 | MIDI permission/model contracts exist, and an MCP MIDI-input recording request still fails explicitly before approval or transport can imply capture. A Windows-only source adapter now uses the OS MIDI selectors and `DeviceInformation.Id` interface identity to feed the all-or-nothing direction-qualified registry; it publishes no partial initial registry, balances WinRT apartment lifetime on the service thread, drains in-flight callbacks on stop and fails disconnected on an unexpected watcher stop. The adapter is unbuilt/unexecuted on this macOS host, so no Windows API/device discovery/hot-plug evidence exists. Discovery remains visibility, not authority or capture capability; no port open, timing, recording/take mutation, permission acceptance or physical device evidence exists. |
| AUD-04 | Multitrack audio recording and monitoring | ⬜ Missing | P0 | Project/take/storage concepts exist; microphone capture and monitoring do not. |
| AUD-05 | MIDI recording, punch, loop and count-in | ⬜ Missing | P0 | Transport and take models exist; real input capture/timestamping do not. |
| AUD-06 | Float32 graph and supported project rates | ✅ Verified | P0 | Float32 WAV kernels and 44.1/48/96 kHz schema limits are tested; managed-preview playback reopens the shared device at the project rate. Complete live device-graph coverage remains AUD-02. |
| AUD-07 | 960 PPQ, tempo/sample conversion and integer samples | ✅ Verified | P0 | Multi-tempo conversion and rounding boundaries are covered by time tests. |
| AUD-08 | Automatic recording and plug-in delay compensation | ⬜ Missing | P0 | Device latency fields exist; graph compensation and golden timing tests do not. |
| AUD-09 | Built-in sampler, drum rack and subtractive synth | 🟠 Scaffolded | P1 | Devices appear in the browser/model; production voice engines and presets are absent. |
| AUD-10 | Built-in effects and analysis suite | 🟡 Partial | P1 | Device catalog/UI and native gain/EQ/compressor/limiter-style kernels exist. Complete real-time implementations and goldens for every promised effect remain. |
| AUD-11 | Signalsmith time stretch and resampling | ⬜ Missing | P0 | Revision is pinned but production integration and goldens are absent. |
| AUD-12 | Deterministic offline rendering | ✅ Verified | P0 | WAV master/stem/audition outputs are deterministic in focused tests. Complete device/automation graph parity remains. |
| AUD-13 | Native crash recovery and degraded operation | 🟢 Working | P0 | Controller falls back to deterministic offline mode and times out native calls. Add forced packaged crash/restart/recording recovery evidence. |

Evidence: [native service](../native/src/audio_service.cpp), [DSP](../native/src/dsp.cpp), [audio controller](../src/main/audio-engine.ts), [native protocol tests](../tests/native/protocol.test.ts), [musical time tests](../tests/core/time.test.ts).

## 6. VST3, CLAP, and ASIO boundary

| ID | Feature | Status | Priority | Current truth and exit criterion |
| --- | --- | --- | --- | --- |
| PLG-01 | Pinned VST3/CLAP native dependencies | 🟢 Working | P0 | Auditable revisions and opt-in fetch configuration exist. Complete license/source/SBOM audit before shipping SDK-derived binaries. |
| PLG-02 | Disposable x64 scanner with timeout | 🟡 Partial | P0 | Scanner process/protocol and catalog ingestion exist. Darwin arm64/x64 build/package composition, standard system/user VST3/CLAP roots, deterministic `Contents/MacOS` selection and a harmless fixture through the real scanner pass while retaining Windows `Contents/x86_64-win`. The host had no VST3/CLAP; six system AU components were excluded because AU is unsupported product-wide. Owner scan cancellation is now terminal across injected empty discovery, last-candidate settlement and final persistence, but it deliberately does not claim helper-process preemption or externally invisible plug-in effects. The helper still reports no SDK adapter. SDK-backed metadata enumeration/load/process, native cancellation acceptance, a real plug-in corpus and complete malformed/crash/hang fixtures are absent. |
| PLG-03 | Stable IDs, version/hash tracking and quarantine | 🟢 Working | P0 | Catalog descriptors, SHA/version and quarantine fields exist. Candidate results stay private until one complete global replacement; cancelled jobs report exact discovery/helper/candidate/catalog phases, including retained resolved writes versus unchanged rejected atomic writes. A successful empty scan still publishes an empty catalog. Add real plug-in corpus and packaged rescan persistence acceptance. |
| PLG-04 | Per-instance bridge with shared audio/MIDI memory | 🟠 Scaffolded | P0 | Bridge process boundary exists; no SDK-backed instance, named-pipe control or shared-memory stream is implemented. |
| PLG-05 | Crash bypass and degraded project recovery | 🟢 Working | P0 | Manager can bypass/degrade a crashed device and preserve state. Add synthetic bridge crash acceptance in a packaged graph. |
| PLG-06 | Generic parameters, presets and automation IDs | 🟡 Partial | P1 | Source-only reducer acceptance enforces declared mutable-device shapes plus parameter key/ID, descriptor, finite ordered min/max and current/default bounds; exact endpoint sets, attribution and inverse lifecycles pass. Authenticated MCP parameter/preset/bypass/remove use fair cost-one admission, queued-zero-transaction cancellation and the canonical lock/revision/atomic commit path. Separately, approved catalog-descriptor instantiate is terminal across waiting/running/late settlement, checks destination-track/device human locks and current target validity, and commits at most one attributed atomic device without native hosting or automatic retry. Host discovery, SDK-backed instantiation, sample-accurate automation and preset round-trip remain. |
| PLG-07 | Managed native plug-in editor windows | ⬜ Missing | P1 | No production plug-in GUI hosting exists. |
| PLG-08 | Separately licensed ASIO bridge ABI | ⬜ Missing | P2 | The repository intentionally excludes ASIO. Define/test a stable external ABI before separate distribution. |

Evidence: [dependency lock](../native/dependency-lock.json), [scanner](../native/src/plugin_scanner.cpp), [bridge](../native/src/plugin_bridge.cpp), [plug-in manager](../src/main/plugin-manager.ts).

## 7. Removed generation/provider product surface

| ID | Former feature | Status | Priority | Current truth |
| --- | --- | --- | --- | --- |
| GEN-01 | Provider-neutral request/capability/job model | 🗑 Removed | — | Runtime/contracts/UI/MCP support deleted; not a release gate. |
| GEN-02 | Immutable generation candidates | 🗑 Removed | — | Candidate types, routes, jobs, and UI deleted. |
| GEN-03 | ElevenLabs adapter | 🗑 Removed | — | Adapter and provider calls deleted. |
| GEN-04 | Stability adapter | 🗑 Removed | — | Adapter and provider calls deleted. |
| GEN-05 | Lyria adapter | 🗑 Removed | — | Adapter and provider calls deleted. |
| GEN-06 | OS-protected provider credentials | 🗑 Removed | — | Credential API/store and Keychain/DPAPI/safeStorage dependency deleted. |
| GEN-07 | Engine-owned generation provenance workflow | 🗑 Removed | — | New workflow deleted. Historical schema/reducer records remain passive compatibility data only. |
| GEN-08 | Provider retry/substitution policy | 🗑 Removed | — | No provider request exists to retry or substitute. |
| GEN-09 | Candidate comparison | 🗑 Removed | — | Candidate UI and auditory workflow deleted. |
| GEN-10 | Source separation adapter | 🗑 Removed | — | Adapter and MCP action deleted. |

Evidence: [ephemeral authority](../src/main/mcp-ephemeral-authority.ts), [MCP contract](../src/main/mcp-host.ts), [renderer controls](../tests/renderer-browser/controls.spec.ts), and passive compatibility tests in [core tests](../tests/core).

## 8. Media and interchange

| ID | Feature | Status | Priority | Current truth and exit criterion |
| --- | --- | --- | --- | --- |
| IO-01 | Controlled import boundary and metadata validation | ✅ Verified | P0 | Path-based import, MIME/size/hash/duration/channel/rate checks and no inline MCP bytes are covered. Authority-approved import now has producer-specific terminal cancellation: files remain sequential, each source/cache/transaction/source-registration/warning phase is reported independently, and earlier cache/commits survive later warnings without request-wide atomicity, cleanup, rollback or retry. Direct authorized import stays synchronous and job-free. The source-only project boundary also rejects malformed schema-declared media-asset metadata on add and stored-integrity validation without inferring path/storage relationships. Expand malformed compressed-media corpus. |
| IO-02 | WAV import and decode | ✅ Verified | P1 | PCM/float WAV decoding and managed asset placement are tested. Expand extensible/multichannel format handling. |
| IO-03 | FLAC/MP3/AAC/OGG import | 🟡 Partial | P1 | Metadata probing/catalog admission exists; a real local AudioToolbox AAC/M4A 48 kHz stereo file passes Darwin admission and compressed analysis fails explicitly. Production sample decoding and render integration remain product-wide incomplete. |
| IO-04 | WAV/FLAC/MP3 master and stem export | 🟡 Partial | P0 | WAV master/stems work and are deterministic. Authenticated running cancellation is terminal but cooperative: injected late master success/failure and one-file stems retention report exact partial output without resurrection, cleanup or automatic retry; waiting approval cancellation writes nothing and releases the global gate. FLAC/MP3 intentionally fail rather than substituting a codec. |
| IO-05 | Standard MIDI import/export | ✅ Verified | P1 | Notes, CC, pitch bends and project PPQ conversion have round-trip coverage. |
| IO-06 | DAWproject 1.0 interchange | 🟡 Partial | P1 | Structured archive and explicit plug-in fallback report exist. Content-addressed media is emitted once when several project assets/clips reference the same archive path; a real ZIP central-directory regression rejects duplicate names, and a same-path/different-payload collision fails before writing an archive. Full schema conformance and automation/device fixture corpus remain. |
| IO-07 | Explicit fallback reports | 🟢 Working | P0 | DAWproject/export warnings name unsupported devices/details. Add per-automation reason codes and restart-persistent report UI. |
| IO-08 | Portable project pack/unpack | ✅ Verified | P0 | See FND-07; authority, overwrite behavior, terminal approved cancellation, sequential entry effects, best-effort removal outcome and later workspace/controller settlement are covered headlessly. |
| IO-09 | Asset analysis cache | 🟡 Partial | P1 | WAV analysis produces JSON, waveform and spectrogram assets and now uses fair authenticated cost-one admission. Queued cancellation performs no source/cache/project effect; a fault-injected listener proves running cache-before-atomic-commit behavior and authenticated asset registration. Tempo/key/transient/peak/LUFS/spectrogram fidelity and compressed-media analysis remain incomplete. |
| IO-10 | Exact-path authority and overwrite policy | ✅ Verified | P0 | Canonical roots, out-of-root rejection and exact existing-path overwrite decisions are tested. Add reparse/junction race corpus. |

Evidence: [media manager](../src/main/media-manager.ts), [export manager](../src/main/export-manager.ts), [media tests](../tests/main/media-manager.test.ts), [persistence tests](../tests/main/persistence.test.ts).

## 9. Desktop UX and accessibility

| ID | Feature | Status | Priority | Current truth and exit criterion |
| --- | --- | --- | --- | --- |
| UX-01 | Dark approachable studio shell and actor language | 🟢 Working | P1 | Rounded dark shell, actor colors, activity/jobs/agents and responsive panels are packaged. Complete spacing/contrast/window-size polish. |
| UX-02 | Transport, engine, CPU and latency status | 🟡 Partial | P0 | Transport controls drive background-prewarmed managed-preview WASAPI playback and render native readiness/latency or explicit offline diagnostics. Metronome/count-in toggles now commit canonical state; unavailable recording is explicitly disabled instead of failing after a click. Real callback CPU/xrun/meter data, recording and device controls await AUD-02/03. |
| UX-03 | Song/SFX workspace switching and browser | 🟢 Working | P1 | Shared project model drives mode-specific browser/editor surfaces. Catalog searches now reset when switching tabs, and audition-kind audio is listed and playable. Add full packaged state-restoration and SFX acceptance. |
| UX-04 | Native menus/dialogs and keyboard shortcuts | 🟡 Partial | P0 | The application-menu button, File/Edit/View menus, dirty-close confirmation and common shortcuts exist. Current `4CF335C1…B16EC7` passed New Project Cancel plus Ctrl+Z/Ctrl+Y; `58E232A3…ED476` passed Escape/history, `CFCC…30621` proved dirty Cancel/Save, and historical `E558…EC412` passed authorized disposable-project Discard. The remaining native menu/dialog matrix still needs coverage. |
| UX-05 | Keyboard-only operation and focus visibility | 🟡 Partial | P1 | Semantic controls and focus styling exist; document tabs expose separate keyboard-operable activation and close controls, an AST regression rejects enabled inert buttons, and packaged Ctrl+Z/Ctrl+Y after text-field blur produced matching canonical undo/redo revisions. Complete timeline/piano-roll alternatives, focus order, traps and shortcut documentation. |
| UX-06 | Screen reader, high contrast and 200% scaling | ⬜ Missing | P0 | No formal Windows accessibility matrix exists. |
| UX-07 | High-DPI, multi-monitor and input-device QA | ⬜ Missing | P0 | No supported hardware/display matrix exists. |
| UX-08 | Renderer failure containment and diagnostics | 🟡 Partial | P0 | IPC/schema boundaries reduce crash surface, but a renderer recovery boundary and diagnostic export are absent. |

Evidence: [editor shell](../src/renderer/App.tsx), [styles](../src/renderer/styles.css), [preload boundary](../src/preload/preload.ts), [packaged E2E](../e2e/desktop.spec.ts).

## 10. Testing, hardening, documentation, and release

| ID | Feature | Status | Priority | Current truth and exit criterion |
| --- | --- | --- | --- | --- |
| QA-01 | Schema/migration/reducer inverse property coverage | 🟡 Partial | P0 | Important operation families and corruption regressions pass. Legacy generation-provenance schema/reducer validation remains passive compatibility coverage for already-authored files; it is not an executable generation workflow. Broader relationship/property/fuzz coverage remains. |
| QA-02 | Native DSP golden/null/timing suite | 🟡 Partial | P0 | Focused DSP kernels and protocol pass, including a live revision commit that must retain the playing state and nonzero cursor. Full effects, routing, PDC, resampling, stretch and recording goldens remain. |
| QA-03 | Synthetic VST3/CLAP fixture matrix | 🟠 Scaffolded | P0 | Process protocol fixtures exist; SDK-backed malformed/crash/hang/latency/state/sidechain/automation fixtures do not. |
| QA-04 | Complete MCP contract/concurrency matrix | 🟡 Partial | P0 | Auth, cold-client discovery, bounds, idempotency, 32 sessions, public reads/subscriptions, owner-scoped jobs, human locks, approvals, retained scheduler families, and approved file/plugin producers have focused headless coverage. Generation/provider suites were removed. Packaged repetition, recording, native-helper cancellation/preemption, and the remaining job/session matrix remain. |
| QA-05 | Provider mock failure matrix | 🗑 Removed | — | Provider adapters and request paths were removed; this is no longer a release gate. |
| QA-06 | Packaged cross-surface scenario matrix | 🟡 Partial | P0 | Historical exact-subject macOS Level 2 passed core Song/SFX, UI↔MCP, recovery, approval, trace, lock, and reattach workflows. Changed bytes require fresh evidence; headless SFX editing, render-match, and broader platform subjects remain open. |
| QA-07 | Corrupt/untrusted persistence and media corpus | 🟢 Working | P0 | Traversal, archive ratios, malformed tail, missing/hash media and bounds are tested. A deterministic track-hierarchy cycle slice now proves schema/integrity rejection of a fully reciprocal stored folder cycle, pre-mutation reducer rejection of descendant parenting, unchanged in-memory state, persisted-read rejection without byte repair and successful reopen after valid bytes are restored. This closes only the already-declared hierarchy-cycle family; audio-output/send/device routing cycles remain unclaimed where policy is unresolved. ZIP64 >4 GiB, codec fuzzing, interrupted recording and the remaining graph corpus remain. |
| QA-08 | Machine-readable performance gate | 🟢 Working | P0 | The local source-level gate passes 200 tracks/10,000 clips, forty viewport projections and 20,000 real reducer commits in 69.6 s while retaining complete journal/trace semantics. Real-time callback/xrun, packaged frame pacing and eight-hour pinned-machine soak remain. |
| QA-09 | Three-level independent Luna/high QA workflow | 🟢 Working | P0 | The newest formal result is strict macOS Level 2 `PASS` `20260810T061608Z-macos-level2`: report `3F902EDD…5E4C`, manifest `092D52EB…63B1`, subject `AE5F9D80…B7E`, executable `4673F18E…1EC7`. It passed exact-subject automation, MCP/Computer Use in both directions, durable discard/recovery, globally serialized approvals and cleanup with no finding. Current formal automation now emits only hash-bound schema-2 observations with `acceptanceVerdict: null`; a caller-pinned read-only verifier, separate from the producer, must derive any automated disposition. Prior FAIL/BLOCKED roots remain immutable history; Level 3 remains open. |
| QA-10 | Exact-build isolated MCP/UI identity harness | 🟢 Working | P0 | Node-24/package verification, exact hash/PID/URL/path/instance/profile checks, authenticated MCP, native Computer Use, cross-surface commits, graceful stop and redaction passed for certified `4CF335C1…B16EC7`. Independent macOS run `20260810T041234Z-macos-level2` passed the repaired package-once/disjoint-evidence workflow and both tester-owned sandbox status gates: denied Darwin `EPERM` continued only through verified package subject, exact connection/session/health PID-instance-profile-URL and authenticated `initialization_required`; no unsandboxed tester-status substitution occurred. The run's overall FAIL arose later from recovery and approval findings, so it is not a certificate. `9F82CC2C…A758C` retained exact coordinator PID/instance/profile/hash/window identity and stopped-redacted cleanup but could not acquire a safe Computer Use target; the separate AGT-04 no-window run passed two protected-root headless cycles. Shared owner-private ACL validation gates start/init and every follow-up; persisted canonical/device/inode identity plus sticky boundary revalidation rejects drift before sensitive action. The default real-Windows helper passes its isolated ACL integration. The opt-in, unwired ABI-v2 Node-API provider has retained real-Windows PASS evidence at `test-results/20260807T060604Z-qa10-node-api-provider-acl-fixture-restoration`: all eight native scenarios, explicit authority, contained CWD, isolated/revalidated ACL fixtures and zero sensitive boundary counters passed. Seven incident artifacts remain untouched. This earns provider-only native acceptance, not coordinator closure, normal staging, package verification or certification. Thirty-three focused launch-free tests pass. The decision-ready integration proposal recommends a separately sealed/signed companion bundle, coordinator-only build profile and nested bootstrap/operational leases, with a complete source/Windows/package acceptance matrix. User authorization is still required before any loader, coordinator, build or package change; the provider remains explicit-path, opt-in, unstaged and unwired. Formal Levels 2/3 remain separate. |
| REL-01 | Honest prerelease version and tracker parity | ✅ Verified | P0 | Root/core/lock/README parity and premature-stable prevention are automated. |
| REL-02 | Hardened package directory | ✅ Verified | P0 | The schema-2 producer binds manifest-authorized clean source plus executable/ASAR/native services without storing acceptance. The independent verifier re-hashes every component and directly requires the exact twelve-tool provider-free runtime, bundle/signature/architecture, runtime entries, component-size floor and hardened fuse policy. |
| REL-03 | Windows CI evidence | ⬜ Missing | P0 | No hosted workflow/run is established. |
| REL-04 | Installer, portable ZIP, checksums and license report | 🟡 Partial | P0 | Forge makers and artifact scripts exist. Historical Darwin arm64 development packaging passed layout/hash/fuse/ad-hoc-signature checks. A current immutable subject, distributable artifacts, clean-machine lifecycle, Developer ID/notarization, and formal release output remain open. Protected-storage signing/profile work is retired. |
| REL-05 | README, architecture, security, QA and contributor docs | 🟢 Working | P1 | Core documents exist and link to literal status. Add contributor guide, troubleshooting and exact development recipes. |
| REL-06 | Dependency/native SDK license and SBOM review | 🟡 Partial | P0 | Fresh production and explicit complete-tree npm audits report zero advisories; 743 package signatures and 136 attestations verify. The release gate now cannot omit dev/optional/peer tooling, and its patched override APIs have a production-resolution regression. Fresh release-time registry revalidation, VST3 terms, exact packaged binaries, SBOM/license output and UNKNOWN-license disposition remain. |
| REL-07 | Tag-driven public `1.0.0` release | ⬜ Missing | P0 | Repository owner/remote, CI, signing, tag and release artifacts are not established. |
| REL-08 | Portable Git snapshot and macOS development structure | 🟢 Working | P1 | The allowlisted initial-snapshot and portability checks remain. Historical arm64 plus x64/universal composition evidence is exact-subject only. Hosted CI, x64-hardware/universal per-slice runtime, SDK plug-ins, physical hot-plug, Developer ID/Gatekeeper/notarization/stapling/archive/update, and Level 3 remain open; no release claim is made. |

Evidence: [test suite](../tests), [QA session identity fault tests](../tests/scripts/qa-session-faults.test.ts), [QA evidence atomic-recovery tests](../tests/scripts/qa-evidence.test.mjs), [QA readiness/pre-signal race tests](../tests/scripts/qa-lifecycle.test.mjs), [QA post-signal cleanup tests](../tests/scripts/qa-stop-cleanup.test.mjs), [QA attach/show race tests](../tests/scripts/qa-show-lifecycle.test.mjs), [single-instance routing tests](../tests/main/single-instance.test.ts), [testing workflow](TESTING.md), [independent tester prompt](testing/LUNA_HIGH_PROMPT.md), [QA session harness](../scripts/qa-session.mjs), [QA MCP client](../scripts/qa-mcp.mjs), [release checklist](RELEASE_CHECKLIST.md).

## 11. Explicitly deferred from v1

These remain outside the selected Windows-local v1 even though a source-transfer/macOS structural verification lane now exists:

- macOS production runtime/package/release, Linux, web, mobile and remote hosted engine operation;
- AIMuse accounts, telemetry, cloud sync and remote multi-human collaboration;
- built-in chat/LLM hosting, video spotting, notation/MusicXML and proprietary DAW-session round-tripping;
- surround/Atmos, spectral repair, ARA, AU/LV2/VST2, MIDI 2/MPE, voice cloning and custom model training.

## Recommended critical path

1. Complete AUD-02 shared/exclusive live-graph fidelity, then implement AUD-03–05, AUD-08 and AUD-11 so playback/recording/MIDI timing is trustworthy before deep editor polish.
2. Complete PLG-02–07 behind the existing isolation boundaries and synthetic fixture matrix.
3. Finish Song daily workflows: audio gestures, take comping, real mixer/automation, built-ins and master verification.
4. Complete codec/DAWproject/analysis fidelity and keep built-in provider/generation surfaces absent.
5. Exercise QA-09/10 Levels 1 and 2 repeatedly while closing accessibility, corrupt-input and packaged regression gaps.
6. Run the pinned performance/soak, clean Windows VM installer, SBOM/license/advisory review and a fresh independent Level 3 against one checksummed candidate.
7. Promote to `1.0.0` only after every selected-v1 P0 is Verified and every P1 exit criterion is satisfied.

## Maintenance rule

Every feature change must update this file in the same change set. Record the automated acceptance that justifies **Verified**. Any material change to the human or agent surface must also update [PARITY_AUDIT.md](PARITY_AUDIT.md). Design documents may remain aspirational; this tracker must remain literal.

## 2026-08-21 zero-per-launch MCP client reconnection

- One static product-owned stdio launcher setup now works for ordinary GUI and supported headless engine launches. It contains only the stable target-engine profile, no Chromium profile switch, bearer or per-launch identity, and is never rewritten by AIMuse. Entry preparation prohibits macOS activation before validation, derives and creates only a deterministic real link-free bridge sibling (owner-private on POSIX), rejects POSIX symlinks and injected Windows case/reparse/junction/filesystem-ID aliases, revalidates identities around `app.setPath`, and explicitly exits without engine-profile fallback on failure. Exact installed arm64 bridge process/alias checks pass; visual no-flash and interactive GUI proof remain pending.
- A dedicated user-private atomic run-state binds fresh engine authority to exact PID, instance, profile, URL and start identity. The bridge validates process liveness plus authenticated health, rejects stale authority/session reuse, and reinitializes automatically after restart. Dead-PID residue can be replaced; a live-PID record with unverifiable identity blocks competing publication as ambiguous. Every TCP path now requires bearer authentication.
- Source/headless lifecycle coverage starts the static client before the engine, connects after launch, stops/restarts the engine with changed authority, proves old bearer/session rejection, reconnects through unchanged client bytes, and verifies clean run-state removal. Protocol regressions also prove exactly-once same-session notification-GET recovery after clean EOF/error and DELETE of a session whose deferred initialize completes after bridge close. A relocated ad-hoc arm64 package and real command-line protocol client additionally pass simultaneous clients, 65-second idle, force/graceful restart, private state, ambiguous owner, canonical alias and zero-survivor cases; named-client, GUI/visual and non-macOS breadth remains pending.

## 2026-08-20 generative-provider and protected-storage removal

- Built-in generation/provider execution was removed from runtime, common contracts, preload/IPC, renderer, MCP, authority policy, jobs, tests, and documentation. No external provider call, provider credential, candidate route, source-separation adapter, or native generation workflow remains.
- MCP uses a fresh 32-byte bearer per engine start, clears it on listener stop, and requires rotation across restart. The former manual renderer token/settings flow has been replaced by the no-secret stdio bridge above. The explicit caller-owned connection export remains QA-only handoff material.
- macOS Keychain, Windows DPAPI, Linux secret storage, Electron `safeStorage`, provisioning/migration, the native protected-storage addon, and Keychain-specific package/test gates are retired. Ordinary code signing, package identity, hardened runtime, notarization, and stapling remain normal release work. `EnableCookieEncryption` is intentionally disabled.
- Legacy project generation provenance stays schema/reducer-readable only to preserve and recover already-authored data. `ProjectService` rejects provenance and `source=generation` writes from every live UI/MCP direct or branch mutation; no release/provider capability is inferred.
- The original removal-only checkpoint launched no app, helper, device, package, signer, protected store, external network, or paid service. The later release-preparation evidence above is separately scoped local ad-hoc/headless proof, not a formal release certificate; a fresh exact-candidate Level 3 remains required.
