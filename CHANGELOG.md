# Changelog

## Unreleased

- Establish the bounded AUD-03 native MIDI discovery contract without connecting a platform backend. The normal native build now carries an all-or-nothing registry of opaque, direction-qualified backend port IDs and reports its disconnected empty snapshot through the audio hello/controller status. A discovery change advances its generation; a future endpoint open must retain and re-check that generation, so disconnect/reconnect cannot revive a stale endpoint reference. Discovery remains visibility only: it grants no input/output authority and cannot make MIDI capture, transport recording, message I/O, timing, hot-plug, take mutation, OS permission acceptance, hardware support or platform parity true. The current service remains disconnected with no ports.
- Close the bounded AUD-03 false-positive MIDI-input recording gap. `transport_manage` now returns an explicit non-retryable `midi_input_unavailable` result before authority, approval, job creation or transport state can imply capture when no native MIDI backend is connected. A policy permission remains necessary for a future backend but cannot create one. This adds no Windows/MIDI API, device enumeration, endpoint opening, timestamping, recording/take mutation, permission acceptance or hardware claim.
- Make the authenticated AGT-18 mixed-family soak deterministic without changing scheduler behavior. Actor round-robin is established by actual first queued arrival, while later work for an already queued actor remains FIFO and does not reseed rotation; the listener fixture now serializes each seeded HTTP submission to establish that arrival order before it asserts it. This changes no lane count, backpressure, cancellation, project ordering or domain-action semantics.
- Close the bounded AUD-02 callback-telemetry fidelity gap. Native transport state now reports the latest measured service-callback load ratio and a cumulative count of callbacks that returned strictly after their frame budget; the host adopts only the two coherent values together and fails closed on malformed current-protocol telemetry while retaining an omitted pair from legacy helpers. This is a service-thread budget observation, not a device-driver/hardware-underrun claim. It adds no endpoint selection, fallback, metering, hot-plug, recording, graph, preemption or real-device performance promise.
- Harden the dependency and packaging checkpoint against current registry advisories. An isolated-cache query to the exact npm registry now pins Electron `43.4.0`, the current `latest` and `43-x-y` release, rather than the stale `43.2.0` runtime. The build graph keeps stable Electron Forge `7.11.2` and Packager `18.4.4`, but replaces Packager's vulnerable `extract-zip` with Electron's signed/provenance-published drop-in extractor `1.0.5` and overrides the compatible TAR and temporary-file APIs to patched `tar 7.5.22` and `tmp 0.2.7`. A focused production-resolution regression executes all three substitutions through their existing Forge/Rebuild/node-gyp/editor consumers. `release:windows` now runs separate production and explicit complete-tree audits, including dev/optional/peer tooling even when ambient npm configuration would otherwise omit it; a production-only audit can no longer satisfy the release gate. Fresh npm 11.18 audits report zero advisories on both surfaces, and registry verification confirms 743 signed packages plus 136 attestations. Forge 7-to-6 downgrade and an unadapted Packager 20 override were rejected: the former regresses the supported toolchain, while the latter changes hook calling conventions. With checksum-verified official CMake `4.4.2` supplied only from a temporary directory, the ordinary arm64 development-package workflow now passes the native build, Forge package, hardened-fuse, bundle-layout, ad-hoc-signature and post-package hash gates; the packaged Electron framework reports `43.4.0`. This remains unsigned-development evidence, not an immutable formal subject, Developer ID/notarization proof or release certification.
- Close the bounded QA-01 device-latency corruption gap. The strict project/transaction schemas and canonical reducer now agree that device `latencySamples` is a nonnegative safe integer across stored state, device add/update/move/parameter mutation/direct deletion and track-cascade cleanup. Schema-invalid latency can no longer enter, survive an unrelated mutation or disappear through deletion/cleanup; the established merged-result `device.update` behavior still permits an explicit replacement with a declared value. Existing target/revision, entity, immutable descriptor, mutable-field, parameter and track-cascade diagnostics retain precedence; exact `0..Number.MAX_SAFE_INTEGER` endpoints, device order, authenticated attribution and semantic inverses for valid lifecycles remain compatible. This adds no general repair, latency measurement, unit conversion, PDC, DSP, native discovery/hosting, routing, retention or cleanup policy.
- Close the bounded QA-01 section-range corruption gap independently from markers. The strict project/transaction schemas and canonical reducer now agree that required section start/end ticks are nonnegative safe integers with the existing non-empty forward-range rule across stored state and section add/update/delete. Schema-invalid ranges can no longer enter, repair a corrupt target or disappear through deletion. Existing entity, duplicate-ID, text, revision, missing-target, complete-order and energy diagnostics retain their section-specific precedence; exact endpoints, identical overlapping ranges, reverse explicit order, name/energy independence, attribution and semantic inverses remain compatible. This adds no chronology, ordering rule, uniqueness, total-range coupling, containment, overlap, snapping, arrangement, lifecycle, migration, repair, retention or cleanup policy.
- Close the bounded QA-01 marker-range corruption gap. The strict project/transaction schemas and canonical reducer now agree that marker ticks and optional end ticks are nonnegative safe integers, retaining the existing forward-range rule, across stored state and marker add/update/delete. Schema-invalid ranges can no longer enter, repair a corrupt target or disappear through deletion. Existing entity, duplicate-ID, text, revision, missing-target and order diagnostics retain precedence; exact endpoints, equal ticks, reverse explicit order, optional ranges independent of marker kind, attribution and semantic inverses remain compatible. This adds no chronology, uniqueness, total-range coupling, containment, overlap, snapping, kind/range coupling, migration, repair, retention or cleanup policy.
- Close the bounded QA-01 shared clip-base numeric corruption gap. The strict project/transaction schemas and canonical reducer now agree that audio and MIDI clip starts are nonnegative safe integers, durations are positive safe integers, gain is finite within `-120..24`, fade durations are nonnegative safe integers, and optional loop length is a positive safe integer across stored state, add/update/move/trim/split/delete, track-cascade deletion and take-lane cleanup's canonical postcondition. Corrupt targets cannot be repaired or silently removed, and direct move/trim/split calls reject schema-invalid values. Existing metadata, source/marker and explicit target/range/split diagnostics retain precedence; independent exact endpoints, clip ordering, attribution and semantic inverses remain compatible. This adds no chronology, total-range overflow, overlap, containment, snapping, fade-to-duration, loop-state/duration coupling, musical interpretation, migration, repair, retention or cleanup policy.
- Close the bounded QA-01 audio clip source-descriptor corruption gap. The strict project/transaction schemas and canonical reducer now agree that `assetId` is a shaped existing reference, source start/duration are respectively nonnegative/positive safe integers, and transpose is finite within `-48..48` across stored state, clip add/update/move/trim/split/delete, track-cascade deletion and take-lane cleanup's canonical postcondition. Corrupt targets cannot be repaired or silently removed, and direct trim calls no longer round or clamp schema-invalid sample values. Existing clip/marker/precondition diagnostics retain precedence; exact boundaries, repeated asset references, attribution, ordering and semantic inverses remain compatible. This adds no media-kind or duration coupling, source containment, marker partitioning, ownership, migration, repair, retention or cleanup policy.
- Close the bounded QA-01 audio warp-marker declared-value corruption gap. The strict project schema and canonical reducer/integrity boundary now agree that source samples and project ticks are nonnegative safe integers across stored state, clip add/update/move/trim/split/delete and track-cascade cleanup. Malformed timing can no longer enter, replace a corrupt target or silently disappear through cleanup. Operation paths retain all-marker `EntityBase` diagnostic precedence, stored integrity retains its per-marker metadata/value order, and exact `0..Number.MAX_SAFE_INTEGER` endpoints, duplicate IDs, explicit array order, reverse chronology, attribution and semantic inverses remain compatible. This adds no chronology, uniqueness, containment, source-duration coupling, split partitioning, migration, repair, retention or broader cleanup policy.
- Close the bounded QA-01 comp-segment declared-value corruption gap. The strict project schema and canonical reducer/integrity boundary now agree on shaped track/take-lane IDs and nonnegative safe-integer start/end ticks, while retaining the existing same-track reference and non-empty forward-range rules. Malformed segments can no longer enter, replace a corrupt target, disappear through direct deletion, or be removed by take-lane cleanup. `EntityBase` and relationship diagnostics retain precedence, valid `0..Number.MAX_SAFE_INTEGER` ranges, attribution and semantic inverses remain compatible, and no overlap, selection, ordering, containment, playback, retention, repair or cleanup policy is added.
- Close the bounded QA-01 immutable track-kind corruption gap. The strict project schema and canonical reducer/integrity boundary now agree on the existing `audio`, `instrument`, `midi`, `folder`, `aux` and `master` literals across stored state, add/update/move/delete targets and recursive track cleanup. An otherwise coherent malformed track can no longer enter, mutate or silently disappear, while existing duplicate-ID, parent, mutable-field, master, reference and dependent-record diagnostics retain precedence. Valid attribution, hierarchy, ordering, clip/device/automation behavior and semantic inverses remain unchanged. This adds no kind conversion, routing, ownership, hierarchy retention, lifecycle, repair or cleanup policy.
- Close the bounded QA-01 automation lane/point declared-value corruption gap. The strict project schema and canonical reducer/integrity boundary now agree on lane track-ID and target shapes, point-record and order ID shapes, armed/visible flags, and point nonnegative safe-integer tick/value/curve/tension bounds before add/update/upsert/delete, device cleanup or track-cascade cleanup. Schema-invalid automation can no longer enter, mutate or silently disappear, while attribution, explicit lane-add order, chronological upsert order and semantic inverses remain unchanged. This adds no target-specific value range, device-parameter existence, cross-track ownership, curve/tension coupling, map-key/entity-ID coupling, chronology-on-create, repair, retention or general cleanup policy.
- Close the bounded QA-01 device immutable-descriptor corruption gap. The strict project schema and canonical reducer/integrity boundary now agree on device track-ID shape, format, optional built-in kind and plug-in ID/version/hash/vendor ceilings before target lookup, add/update/move/parameter mutation, direct deletion or track-cascade cleanup. Malformed stored descriptors can no longer be normalized or silently removed; valid attribution, routing membership, parameters and semantic inverses remain unchanged. Empty optional strings, non-digest hashes within the declared length and independent format/built-in/plug-in fields remain schema-compatible. This adds no format coupling, hash verification, native discovery/loading/hosting, routing-cycle, state-media retention, repair or cleanup policy.
- Close the bounded QA-01 generation-provenance immutable declared-value corruption gap. The strict project schema and canonical reducer/integrity boundary now agree on primary/reference asset ID shapes, provider/model/kind/prompt/lyrics/request/cost/currency/rights fields and the experimental flag; malformed registration and stored/update-target records fail before media lookup or mutation. Exact declared boundaries, schema-permitted duplicate reference IDs and empty optional strings, authenticated attribution, existing media references, mutable provenance updates and semantic inverses remain unchanged. This adds no provider-kind compatibility, reference or request uniqueness, billing/currency interpretation, provider execution, repair, retention, cleanup or retry policy.
- Add a bounded QA-07 track-hierarchy cycle corruption contract without changing production behavior. The existing schema/integrity validator rejects a fully reciprocal stored folder cycle, `track.move` rejects descendant parenting before mutation, and persisted folder reads reject the same corrupt graph without repairing its bytes; restored valid bytes reopen normally. This evidence does not define audio-output/send/device routing cycles, hierarchy deletion/retention, migration, repair or cleanup policy.
- Close the remaining declared `EntityBase` family gap for SFX deliverables. Stored records and the existing add/update/delete reducer boundaries now reject malformed entity/creator/updater IDs, revisions and UTC ISO timestamps before normalization, mutation or deletion. Valid authenticated attribution, revision increments, SFX values, lifecycle and semantic inverses are unchanged, so every currently declared `EntityBase` record family now has this corruption boundary. No timestamp/revision ordering, map-key coupling, actor lookup, repair, cleanup, cascade, retention, deletion or new lifecycle policy is added.
- Close the bounded QA-01 media/history `EntityBase` corruption gap. Stored media assets, generation provenance, checkpoints and variants, plus every existing add/register/update/delete boundary for those families, now require declared entity/creator/updater IDs, nonnegative integer revisions and UTC ISO timestamps before normalization or mutation. Exact independent boundaries and reverse-ordered valid timestamps remain accepted; authenticated attribution, references, lifecycles and semantic inverses are unchanged. This does not add timestamp/revision ordering, map-key coupling, actor lookup, cleanup, retention, cascade, rollback or new checkpoint/variant policy.
- Give authority-approved `project_manage unpack` its own terminal compound continuation instead of resurrection-prone generic settlement. Waiting owner cancellation releases approval capacity before archive access, destination creation, extraction, validation or project open. Running cancellation is terminal but non-preemptive: owner-only partials distinguish archive-read state, discovered entries, started/completed sequential file writes, created directories, destination and cleanup state, extracted-project validation, and later workspace/opened-project/controller settlement. A successful validated extraction remains even when open warns or controller synchronization fails; an extraction failure reports whether the existing removal attempt completed or failed, and failed cleanup can retain partial multi-file output. An archive-open rejection now enters that same best-effort removal path instead of leaking its newly created root, without turning cleanup into a guarantee. Direct authority-allowed unpack remains synchronous/job-free, and unpack stays outside AGT-18 fair lanes. AIMuse promises no archive-wide atomicity, cleanup, rollback, preemption or automatic retry. A six-test bearer-authenticated listener passes in eight independent pinned-Node-24 processes (48/48 race tests) through injected archive-open, entry-write, cleanup and `ProjectService`/controller seams, proving foreign-ID privacy, approval-gate reuse, direct-authority compatibility, partial output, late success/failure monotonicity, retained workspace re-observation and zero `audio.start()` or native/helper execution.
- Give authority-approved media import its own terminal continuation without admitting the approval wait or sequential multi-file loop into AGT-18 fair lanes. Waiting owner cancellation releases approval capacity before source reads, cache copies or project transactions. Once running, cancellation is terminal but non-preemptive: each request index reports source-read, cache-copy, project-transaction, asset-source-registration and warning state, a late copy/commit cannot resurrect the job, and the service continues after per-file warnings. Completed cache output and earlier/later authenticated commits remain; returned lock/conflict/duplicate outcomes prove no file transaction, while an engine error or rejected transaction stays conservative because recovery-journal append may have preceded failure. Direct authority-allowed import remains synchronous/job-free. AIMuse does not promise request-wide atomicity, cleanup, rollback, preemption or retry. A five-test bearer-authenticated listener matrix passes in eight independent pinned-Node-24 processes (40/40 race tests), proving foreign-ID privacy, approval-gate reuse, direct-authority compatibility, partial cache output, sequential partial success, late copy/commit/source registration, exact returned conflict, conservative late engine error, terminal late cache-preparation rejection, ordinary failure/no-retry behavior, attribution and zero `audio.start()` or native-helper execution through injected file/cache and `ProjectService` seams.
- Give authority-approved plug-in instantiate its own terminal continuation without admitting it into AGT-18 mutation lanes or claiming native hosting. Waiting owner cancellation releases the approval gate before the authority continuation and before any project mutation. Once running, cancellation is terminal but does not preempt its one atomic `device.add` project transaction: a late returned commit retains the authenticated descriptor device, a returned human-lock/current-target conflict commits nothing, and an exceptional rejection remains conservatively uncertain for re-observation; no path retries automatically or resurrects the cancelled job. `ProjectService` lock detection now includes a `device.add` operation's nested device ID and destination track so approved and direct agent instantiation cannot bypass a human lock. A four-test bearer-authenticated listener matrix passes in eight independent pinned-Node-24 processes (32/32 race tests), proving foreign-ID privacy, approval-gate reuse, direct allowlist/no-job compatibility, attribution, lock and stale-target validation, late commit/failure settlement, and zero `audio.start()` or native-helper execution through injected catalog/project/audio seams. Catalog and scan retain their separate global/catalog-job semantics, and descriptor registration remains distinct from absent SDK-backed plug-in instantiation/hosting.
- Keep an owner plug-in scan job terminally cancelled across empty discovery, last-candidate settlement, final catalog persistence and late writer rejection. Owner-only `result.partial` now distinguishes discovery (`not-started`, `may-have-completed`, `completed`), native-helper call state (`not-started`, `unconfirmed`, `settled`), private candidate results (`none`, `partial`, `complete`) and the globally shared catalog (`unchanged`, `write-may-have-completed`, `replaced`). Candidate metadata is accumulated privately and never published as a partial catalog: cancellation before final persistence keeps the previous catalog, while cancellation during the atomic write first reports uncertainty and may later retain the complete replacement if that write resolves; an atomic-writer rejection settles back to unchanged. A normal empty scan still intentionally replaces the catalog with an empty catalog. AIMuse does not claim helper preemption or external-effect visibility, clean up, or retry automatically. A four-test bearer-authenticated listener matrix proves owner privacy, empty/last-candidate terminality, late replacement/failure monotonicity and zero `audio.start()` or helper execution through injected discovery/scanner/writer seams. Scan remains outside AGT-18 device-edit admission, and instantiate retains its separate approval contract.
- Keep an authority-approved single-path project-open job terminally cancelled when its continuation returns or rejects late. Waiting cancellation releases the global approval gate before source read, workspace/opened-project or audio-controller effects. Running cancellation is cooperative and initially reports those phases as uncertain without preemption, rollback or retry. A returned opened ID settles to retained workspace/project plus synchronized controller only when settlement-time project presence and controller project/revision confirm both; a returned warning-only result settles to exact zero workspace/project/controller effects. Rejection or concurrent state that cannot be confirmed stays conservative because `ProjectService.open` can add and activate a project before synchronization fails. A four-test bearer-authenticated listener matrix repeats in eight independent processes and proves foreign-ID privacy, approval-gate reuse, direct authority-allowed no-job compatibility, retained workspace re-observation and zero native/audio startup. Compound unpack, media import and authority-gated plug-in instantiate now have their distinct contracts above; recording retains its separate unresolved effects.
- Keep an authority-approved project-save job terminally cancelled when its continuation completes or fails late. Waiting cancellation releases the global approval gate before destination writes, project clean-state/path publication, or `file.saved` audit recording. Running cancellation is cooperative: it reports destination/project/audit effects as uncertain, does not preempt or clean up persistence, and never retries automatically. A late successful save remains cancelled but settles to retained destination output, saved project state and the authenticated destination-free audit; a continuation failure leaves destination output, clean-state publication and audit recording conservatively uncertain because failure can occur at different save phases. A three-test bearer-authenticated injected-save listener matrix repeats cleanly in eight independent processes and proves foreign-ID privacy, approval-gate reuse, attribution, durable output/audit re-observation and an injected failure whose actual project/audit state remains unchanged without native, audio or external services. Project open, compound unpack, media import and authority-gated plug-in instantiate have separate contracts; recording retains its unresolved effect boundary.
- Keep an owner generation job terminally cancelled when an injected provider ignores abort or a candidate-cache write finishes late. For an agent running under installed process authority, a valid provider payload that fulfills after cancellation still consumes its fulfilled-request budget unit, but a new post-provider terminal check prevents candidate persistence and project effects; a cache write already in flight may settle as one retained immutable candidate, while generation itself still makes no project transaction. Cancelled results report submitted/fulfilled/late-fulfilled provider counts, `candidateCache` as `unchanged`, `may-be-partial` or `retained`, retained candidate count and `project: unchanged`; owner inspection may change from uncertain to retained, no provider request is retried automatically, and only a later explicit owner acceptance can register the asset source and commit asset/provenance/clip state. A deterministic bearer-authenticated listener regression proves both race boundaries, foreign-ID privacy, installed-authority accounting, candidate immutability, reject-then-accept semantics and asset-source-before-commit ordering without real providers, credentials, audio or native startup. This producer-specific rule does not change generic approved actions, media import, session ownership or plug-in scan, which now has its own catalog/helper contract.
- Keep owner export jobs terminally cancelled when an injected master or multi-file stems operation completes or fails late. Authenticated `job_manage cancel` now routes export jobs through their producer, waiting approval cancellation reports zero destination/project effect and releases the global approval gate, and running cancellation remains cooperative: it does not preempt writes, delete retained output, roll back a possible portable-pack save or retry automatically. Cancelled and failed export jobs report `partial.output` as `unchanged`, `may-be-partial` or `retained` and `partial.project` as `unchanged` or `save-may-have-completed`, with owner-only inspection/destination guidance. A three-test bearer-authenticated listener matrix proves foreign-ID privacy, late success/failure status monotonicity, one-file stems retention, ordinary failure guidance and approval-gate release without `audio.start()`, native helpers or external services. Session DELETE remains scoped to queued scheduler work and presence; it does not silently acquire a new already-created-job cancellation policy.
- Add a long deterministic bearer-authenticated mixed-family AGT-18 scheduler soak without changing production behavior. Each run executes eight fixed seeded rounds across direct/branch project edits, actor history, checkpoint creation, generation rejection, plug-in edits, WAV analysis and audition job creation while four lanes are occupied; it proves queued actor round-robin order, observation bypass, attributed contiguous project commits and no starvation behind injected pending generation/render jobs. Separate phases reach the four-item actor limit and exact 2,048 global queued-cost limit, cancel mixed work by project and by closing its session, preserve human locks, and retain every family’s distinct retry/re-observation or job-partial contract. Eight independent pinned-Node-24 executions pass all three soak tests (64 seeded rounds / 512 queued mixed-family actions). Fairness remains a queued-work guarantee, and deliberate session teardown guarantees queue/effect cleanup rather than delivery of responses on the transport being closed; media import and authority-gated plug-in instantiation remain outside admission.
- Extend AGT-18's shared fair mutation admission to authenticated audition and consolidation render-job creation at cost one. Queued cancellation creates no job, starts no render, writes no cache and invokes no project transaction; an admitted job does not hold its lane while render remains pending. Running job cancellation is terminal but cooperative rather than preemptive: late render output may remain in managed cache, an already-started asset transaction may commit and register its source, and an already-started consolidation clip transaction may also commit. Cancelled and failed jobs record `partial.cache` as `retained` or `may-be-partial` and `partial.project` as `unchanged`, `asset-committed` or `asset-and-clip-committed`; callers inspect that owned job and re-observe before repeating. A bearer-authenticated injected-render matrix proves owner privacy, zero-effect queued cancellation, released lane capacity and each late-effect boundary without starting audio or native helpers. This does not claim render preemption, request-wide/all-files atomicity or broader native-media support.
- Extend AGT-18's shared fair mutation admission to authenticated WAV media analysis at cost one. Queued cancellation performs no source read, managed-cache write or project transaction; once running, analysis remains non-preemptive and writes three content-addressed cache outputs before one atomic three-asset project commit, so completed cache artifacts may remain if a later write or commit fails while project state stays unchanged on commit failure. A five-session bearer-authenticated regression proves discovery, backpressure, zero-effect queued cancellation, exact cache-before-commit failure behavior, later attributed asset registration and retained unrelated edits without file authority, audio rendering or native startup. Import stays outside admission because authority/approval and per-file commits permit partial success; audition/consolidation now admit only job creation under their separate cooperative partial-effect contract.
- Extend AGT-18's shared fair mutation admission to authenticated plug-in parameter, preset, bypass and removal actions at cost one each. Queued cancellation invokes no project transaction; admitted work remains one revision-checked `ProjectService` transaction that preserves human locks, server attribution, serialized commit order and atomic all-or-nothing state change. Catalog observation, scan jobs and authority-gated instantiation deliberately retain their separate lifecycle contracts rather than implying new approval or partial-effect semantics. A five-session bearer-authenticated regression proves discovery, backpressure, zero-effect cancellation, lock priority, contiguous later commits and retained unrelated state without audio or native plug-in startup.
- Extend AGT-18's shared fair mutation admission to owner-scoped generation start, variation, acceptance and rejection at cost one each. Queued start/variation cancellation precedes approval reservation, job creation and credential/provider work; queued accept/reject leaves candidates, asset sources and project state untouched. Admitted provider execution remains an owner-only cancellable job outside the mutation lanes, while acceptance preserves authenticated attribution, commit-only accepted IDs and duplicate protection. A five-session bearer-authenticated regression proves privacy, backpressure, zero-effect cancellation, released approval capacity, later attributed outcomes and pending mock-provider overlap without audio, real credentials or external network.
- Extend AGT-18's shared fair mutation admission to authenticated checkpoint/variant creation, merge and discard while keeping compare read-only. Each non-idempotent action costs one, overflow/cancellation requires re-observation, and queued cancellation occurs before any service invocation or checkpoint-file write. Running work remains non-preemptive and preserves the established checkpoint-before-register branch semantics. A five-session bearer-authenticated headless regression covers mixed-action backpressure, zero-write queued cancellation, retained active commits and attributed checkpoint/variant outcomes without starting audio.
- Extend AGT-18's shared fair mutation admission to authenticated `history_manage` undo/redo. Queued cancellation cannot start or partially apply history, running atomic history commits remain non-preemptive, non-idempotent busy/cancelled guidance requires re-observation, and paired five-session no-socket and authenticated-loopback MCP acceptances cover coexistence with four active `project_apply` lanes plus retained later edits and attributed undo/redo without starting audio.
- Add an allowlisted initial Git snapshot boundary, anchored never-track rules for local coordination/evidence/credential material, portable text/binary attributes and a root-avoiding case/Unicode/path/import audit. No files are staged or committed and no remote is configured.
- Add a truthful macOS development structure: Node-24/Darwin preflight, a macOS-14 source/native-test workflow, platform-aware profile/native naming and CoreAudio protocol identity, CMake runtime separation, and fail-closed Mac packaging. CoreAudio, Keychain, signing, entitlements, notarization, package and UI evidence remain explicitly unearned.
- Record the retained AUD-02 two-phase native PASS: exact Windows build/CTest plus bounded shared and effective-exclusive WASAPI playback both passed with matching requested/effective diagnostics, unchanged endpoint identity, natural exits and no fallback, force or cleanup. Scope remains exact-executable/endpoint/scenario only.
- Owner-scope MCP job summaries, job details and generation job actions so foreign job IDs reveal no result or approval data.
- Add durable destination-free `file.saved` audit events with authenticated human/agent/system attribution, separate from project revision and undo history.
- Add action-specific MCP discovery schemas, structured result/next-step guidance, server workflow instructions, the `aimuse_help` tool and the optional `aimuse://guide` resource.
- Add two-session file-authority/privacy acceptance and cold tools-only discovery contracts.
- Make protected provider-credential rotation/removal a validated same-directory atomic replace, and fail unavailable/throwing encryption/decryption closed with mock coverage for known-good preservation, cross-provider isolation, stage cleanup, sanitized errors, unconfigured capabilities, plaintext non-persistence and zero provider fetch.
- Add a headless renderer/preload/public leakage contract and redact active provider credentials from transport diagnostics before failed jobs reach snapshots, events or MCP reads.
- Add an AGT-04 isolated headless restart harness that creates a Windows ACL-protected evidence root before any bearer exists, verifies two no-window authenticated handoffs on one profile, and requires graceful session closure, instance-bound stop and credential-redacted cleanup.
- Require general QA session start and MCP initialization to validate one explicit contained owner-private evidence root before executable access/launch or bearer read/write, with test-owned rejection coverage for inherited broad access, wrong ownership, unsafe principals, links and path escapes.
- Persist the non-secret private-root canonical/device/inode identity in coordinator manifest/state and revalidate it across every session/MCP follow-up, failing ACL or path-object drift before credential, process, network or evidence activity.
- Add and pass a launch-free one-root Windows integration harness for the production ACL reader: protected owner/SYSTEM/Administrators acceptance, inherited-broad sibling rejection, three stable filesystem-identity observations, credential-free evidence, exact-object failure cleanup and replacement-object cleanup refusal.
- Define and test a strict injected Windows handle-lease contract for QA coordination, including fresh rotation-safe snapshots and an initially proposed handle-relative atomic replacement capability. The later Win32 containment correction supersedes that capability with pinned absolute-path replacement; neither contract implies coordinator wiring.
- Add the approved provider-only in-process Node-API prototype behind an opt-in native target, with an explicit loader and isolated real-Windows race/security harness. It remains unstaged and unwired; native evidence awaits one frozen real-user build/test command and makes no package or certification claim.
- Preserve the first provider-native MSVC `/WX` failure as infrastructure evidence and replace its implicit validated-wide-digit narrowing with an explicit checked ASCII conversion. A launch-free regression assertion locks the warning-free intent; real native evidence moves to a fresh immutable retry root.
- Preserve the cleanly compiled second provider-native run as separate infrastructure evidence after its sole multi-config MODULE artifact appeared below `RelWithDebInfo` and was rejected before load. Pin target-level library/runtime and per-configuration output directories to the exact build root; keep the harness deterministic with no glob or stale-configuration fallback.
- Preserve the third provider-native run as separate infrastructure evidence after it proved the exact artifact path and loaded the addon but stopped at the first legacy `FileRenameInfo` atomic commit with Windows error 87. Pair `FILE_RENAME_INFO::Flags` with documented `FileRenameInfoEx`, use only the replace-if-exists flag, retain the pinned-parent/basename contract and add launch-free structure/class/buffer/fallback coverage; native proof moves to a fourth immutable root.
- Preserve the fourth provider-native run as separate infrastructure evidence after the controlled `FileRenameInfoEx`/flags/buffer correction returned the same Windows error 87, disproving information class as the sole cause. Add an explicit, sanitized ten-case provider-only diagnostic surface for target state, structure tail/termination, parent access/options, legacy/extended comparison and stage options; freeze it under a new absent root without changing production rename semantics.
- Preserve the completed provider rename diagnostic as PASS-for-diagnosis only: all ten explicit-target-parent calls returned error 87 with target/stage invariants intact, ruling out the varied target, buffer, access, option and information-class dimensions. Correct the unvaried same-directory case to `RootDirectory == nullptr` plus the validated simple basename, which stays relative to the stage handle's existing pinned directory; retain the diagnostic's old call shape separately and freeze a fresh provider-only native checkpoint without wiring or packaging the addon.
- Preserve retry4 as a post-rename verification FAIL, not another syscall failure: the null-root/simple-basename call succeeded before the old combined invariant queried the pre-rename handle name. The later five-case diagnostic proved that success published beneath process current directory, not the protected stage parent. The share-zero identity lock and fresh exact-target checks remain valid alarms but cannot substitute for a correctly anchored destination.
- Preserve retry5 as a later post-commit identity FAIL: rename, staged identity, fresh target open, exact path and containment passed before the existing-target handle identified a different object. Select documented POSIX replacement visibility only for the production `FileRenameInfoEx` commit so old handles remain on the displaced object and subsequent target opens bind to the renamed stage; retain every identity/security/byte check, keep the classic diagnostic unchanged, add an explicit displaced-handle acceptance case and freeze retry6 without wiring or packaging.
- Preserve retry6 as the same post-commit identity FAIL after the POSIX flag compiled and rename succeeded, leaving that visibility hypothesis unaccepted. Add a separate diagnostic-only five-case matrix for existing/absent targets, classic/POSIX mode, stage sharing, retained displaced-target binding and pre/post-stage-close observations; enforce a sanitized fixed-ID/Win32-code/boolean schema and keep production fail-closed and unwired pending independent evidence.
- Record the replacement diagnostic's conclusive containment incident: Win32 resolved every null-root basename against process current directory, leaving seven fixed owner-private mock artifacts in the project root and disproving the former run-root-only claim. Correct the opt-in addon to version 2, reject every version 1 binary/descriptor, require a canonical absolute destination reconstructed from the pinned parent, reject stream/relative names, correct the five-case surface, and mark mutation containment as not independently observed by the harness. No native acceptance or cleanup is claimed.
- Close the provider harness's launch-free CWD safety gap: create and owner-protect one retained `native-process-cwd` inside every fresh run root before any addon load/call, identity-check it at callback entry/exit, independently observe directory mutations, require empty before/after snapshots, restore and validate the caller CWD before evidence writes, and fail without cleanup on drift or mutation. Historical reports retain their original unmonitored boundary; native provider acceptance remains unearned.
- Preserve the first contained-CWD native run as infrastructure FAIL before provider cases: containment passed fully, but the shared lease preflight resolved an omitted `test-results` default beneath the run-owned CWD. Bind every provider/diagnostic verifier and the lease contract to the exact explicit run root, reject relative authority/private-root/path inputs or broader authority, and require sanitized `providerAuthorityExplicitlyBound` evidence before acceptance.
- Preserve the explicit-authority native run as separate infrastructure FAIL after addon load: contained CWD passed, but one unlabeled provider snapshot open returned Win32 sharing violation 32 before a nested result was serialized. Keep production access/share and identity checks unchanged; tag every provider-mode snapshot with one fixed lifecycle phase, propagate only phase/code/booleans through clean-CWD failure evidence, and freeze a fresh discriminator root for independent execution.
- Preserve the snapshot-phase run as provider FAIL with conclusive lifecycle evidence: Win32 32 occurred only during the external atomic rename worker after preflight, provider acquisition and provider commit. Add only `FILE_SHARE_DELETE` to fresh read snapshots so the confirmed DELETE lifecycle can coexist; keep write sharing denied, retain every fixed phase, and lock root/parent non-delete-share, stage share-zero and committed-target handle masks with launch-free coverage.
- Preserve the snapshot-delete-share run as a later provider FAIL after the snapshot discriminator disappeared: the final JS authority oracle found the deliberately granted Users SID because `/grant:r` did not remove that fourth explicit inheritable ACE and the sticky test had mutated the primary accepted root. Isolate both ACL-negative fixtures, remove the exact grant recursively, re-protect and identity/authority-validate every affected object, retain sentinel/sticky checks, and emit only fixed phase/boolean evidence for residual ACL leakage.
- Record the independently audited provider-only ABI-v2 native PASS: all eight real-Windows scenarios, explicit authority, contained CWD and isolated two-object ACL restoration checks passed with zero sensitive boundary counters. Keep the addon opt-in, explicit-path loaded, unstaged and unwired; the result is neither coordinator closure nor package/release certification, and production loading/staging/signing requires a separate architecture decision.
- Add a proposal-only QA-10 coordinator integration checkpoint: recommend a separately sealed/signed Node-24 companion bundle, coordinator-only diagnostic-free build profile, exact signed-manifest discovery, nested bootstrap/operational leases, fail-closed lifecycle mediation and explicit source/Windows/package gates. No loader, coordinator, native, build, staging, signing or package behavior changes without later user authorization.
- Sequence native audio shutdown so a successful protocol request gets a bounded natural-child-exit observation window before the existing force fallback is eligible. Retain startup/error recovery and survivor management, preserve preview-work quiescence, and add deterministic launch-free coverage for the exit-event race and timeout path.
- Record exact native acceptance for that shared-WASAPI teardown/cache subject: natural code-0 exit without fallback, pending-refresh quiescence, retained-cache rename and quiet-window stability, unchanged endpoint, zero residual processes and no cleanup. Exclusive/hot-plug/package/app/release scope remains open.
- Add a bounded QA-01 automation integrity/inverse slice: require lane ownership/backreferences, complete point-order membership and existing device targets, and cover attributed lane/point lifecycle operations plus semantic inverse restoration.
- Add a bounded QA-01 device-routing slice: enforce reciprocal device ownership, allowed track kinds, state-media and send/sidechain endpoint references; preserve send/sidechain creation attribution on update; and cover reversible device, parameter, move, cascade and routing lifecycles.
- Add a bounded QA-01 media/history slice: require provenance, checkpoint and variant media/checkpoint references; block deletion of variant snapshot assets; and cover reversible asset/provenance/checkpoint/variant operations without deciding non-active-variant checkpoint retention policy.
- Add a bounded QA-01 take/comp slice: enforce reciprocal same-track clip/take-lane membership and valid comp track/lane/range references; retain comp creation attribution on update; and cover reversible lane, clip and segment cleanup without deciding comp selection, overlap or track-deletion retention policy.
- Add a bounded QA-01 timeline slice: require complete tempo/meter/marker/section order membership, chronological tempo/meter indexes with tick-zero anchors, and valid declared marker/section ranges; preserve tempo/meter creation attribution on upsert; and cover reversible event/marker/section lifecycles without imposing equal-tick precedence, overlap, snapping or editing policy.
- Add a bounded QA-01 track-hierarchy slice: require exact duplicate-free root and sibling membership, reciprocal folder parent/child links, root-only master placement and acyclic complete coverage; cover reversible root/nested add and move/reorder lifecycles without deciding cascade deletion, routing cycles, depth, hierarchy UX or retention policy.
- Add a bounded QA-01 unlaned-clip slice: require duplicate-free reciprocal per-track clip order, track-kind compatibility and declared timeline/source/fade/loop/warp ranges; reject cross-kind split shapes; and cover reversible add/update/move/trim/split/delete lifecycles without deciding overlap, snapping, lane reassignment, audio source partitioning, storage or retention policy.
- Add a bounded QA-01 MIDI-event slice: require exact duplicate-free note/control/pitch-bend map-order identity and every declared timing/value/channel range; cover reversible note/control/bend add/update/delete lifecycles while preserving insertion order and leaving clip containment, overlap, chronology, transformations and editing policy undecided.
- Add a bounded QA-01 SFX-deliverable slice: enforce declared text/format, timeline/variant, loop-point numeric, tail/variation and loudness bounds; cover attributed reversible add/update/delete lifecycles without interpreting rendering, codec behavior, naming expansion, loop semantics, storage, export, UX or retention policy.
- Add a bounded QA-01 project metadata/settings slice: align reducer integrity with the declared project name/kind and settings enums, literals, integer ranges, booleans and loudness bounds; cover attributed reversible rename/settings edits while retaining the existing edit-only crossfade ceiling without introducing defaults, migration, compatibility, persistence, collaboration or UX policy.
- Add a bounded QA-01 lyrics slice: replace reducer-side silent truncation with fail-closed parity at the declared 200,000-character maximum, enforce the same integrity bound, and cover exact reversible set/replace/clear lifecycles without introducing authoring, synchronization, storage, collaboration, UX or compatibility policy.
- Add a bounded QA-01 track mutable-field slice: enforce schema-declared name/color, gain/pan, state-flag and routing-value shapes at direct reducer and integrity boundaries; cover attributed reversible `track.update` across every declared field without changing routing, hierarchy, cascade, naming, propagation, storage, collaboration or UX policy.
- Add a bounded QA-01 marker/section text slice: enforce declared marker name/color/kind and section name/color/prompt values at add, update and integrity boundaries; cover attributed reversible add/update/delete lifecycles while preserving explicit order and leaving arrangement, overlap, endpoint, snapping, interpretation, naming, authoring and UX policy unchanged.
- Add a bounded QA-01 tempo/meter value slice: enforce integer nonnegative ticks, finite 20–400 BPM, step/linear curves, integer 1–32 numerators and exact supported denominators at upsert and integrity boundaries; cover attributed reversible upsert/delete lifecycles while preserving chronological order, tick-zero anchors and undecided equal-tick/musical semantics.
- Add a bounded QA-01 clip mutable-value slice: enforce declared text/color lengths, common boolean flags, fade-curve literals and audio stretch/reverse values at add, update and integrity boundaries; cover attributed reversible MIDI/audio add/update/delete lifecycles while preserving explicit clip order and every existing timing/source/cross-kind policy.
- Add a bounded QA-01 device mutable/parameter slice: enforce declared mutable-field shapes, parameter key/ID identity, descriptor types, finite ordered min/max and current/default bounds at add, update, set and integrity boundaries; cover attributed reversible device/parameter lifecycles without changing ownership, state references, ordering, DSP, preset, plug-in or automation policy.
- Add a bounded QA-01 send/sidechain value slice: enforce finite `-120..24` send gain, declared routing booleans and nonnegative integer sidechain bus indices at upsert/integrity boundaries; cover attributed reversible create/update/delete lifecycles while preserving endpoint checks and leaving self/cycle, graph, DSP and automation policy undecided.
- Add a bounded QA-01 take-lane mutable-value slice: enforce declared name length and active boolean at add, update and integrity boundaries; cover attributed reversible add/update/delete lifecycles while preserving reciprocal ordered clip membership, comp ranges and every active-lane, selection, overlap, retention, reassignment and playback policy.
- Add a bounded QA-01 provenance mutable-value slice: enforce optional model-version and transformation count/item-length bounds at register, update and integrity boundaries; cover attributed reversible register/update lifecycles while preserving asset relationships and leaving provider, cost, rights, retention, deletion, generation and storage policy unchanged.
- Add a bounded QA-01 variant mutable-value slice: enforce declared name, status, nonnegative integer revision and optional snapshot-ID shapes at register, update and integrity boundaries; cover attributed reversible register/update lifecycles while preserving checkpoint/snapshot references and leaving transition, merge/discard, deletion, cascade and retention policy undecided.
- Add a bounded QA-01 media-asset metadata slice: enforce declared kind/name/MIME/hash/byte/storage, optional path-length/audio-shape and source values at add/integrity boundaries; cover attributed reversible add/delete lifecycles while preserving reference guards and leaving path coupling, storage, decoding, retention and cascade policy undecided.
- Add a bounded QA-01 checkpoint declared-value slice: enforce declared name, nonnegative integer revision, snapshot-ID shape, automatic boolean and optional reason bounds at register/integrity boundaries; cover attributed reversible register/delete lifecycles while preserving snapshot/variant relationships and leaving update, automatic meaning, deletion/cascade and retention policy undecided.
- Add a bounded QA-01 activity-ledger slice: enforce declared entry/actor/client/transaction/label/status/timestamp/revision shapes with opaque record details; cover attributed commit/undo/redo history and consistently trim generated entries to 2,000 while retaining the separate 10,000-entry stored compatibility ceiling.
- Add a bounded QA-01 project-root metadata slice: share declared project ID/revision/timestamp/creator/dirty/path validation across creation and stored integrity while preserving authenticated transaction attribution and leaving temporal, revision, save-state, path, identity and migration policy undecided.
- Add the first bounded QA-01 EntityBase slice: enforce declared entity/creator/updater IDs, revisions and timestamps for tempo, meter, marker and section stored records and existing operations while preserving attribution, inverses and ordering without imposing timestamp, revision or map-key coupling policy.
- Extend bounded QA-01 EntityBase coverage to top-level tracks and clips: enforce declared metadata at stored and every existing operation boundary, including split-right input and cascade deletion, while preserving attribution, hierarchy/order membership and semantic inverses without extending the policy to nested MIDI, warp, lane or comp entities.
- Extend bounded QA-01 EntityBase coverage to take lanes and comp segments: enforce declared metadata at stored and existing operation boundaries, preflight related comp records before lane cleanup, and preserve attribution, explicit clip order, comp ranges and semantic inverses without introducing active-lane, overlap, selection, retention or playback policy.
- Extend bounded QA-01 EntityBase coverage to nested MIDI notes, controls and pitch bends: reject malformed metadata at stored, direct MIDI, semantic-target and pre-populated clip-normalization boundaries while preserving declared value contracts, insertion order, attribution and semantic inverses without adding control-update or transformation policy.
- Extend bounded QA-01 EntityBase coverage to audio warp markers: validate stored and every marker-carrying clip boundary, including replacement, split and cascade deletion, while preserving marker metadata/values and semantic inverses without imposing chronology, uniqueness, containment or split-partition policy.
- Extend bounded QA-01 EntityBase coverage to automation lanes and nested points: validate stored, add/update/upsert/delete and track/device cleanup boundaries before normalization or mutation while preserving attribution, targets, point order/data and semantic inverses without adding cross-track, chronology, uniqueness, containment or curve policy.
- Extend bounded QA-01 EntityBase coverage to devices, sends and sidechain routes: validate stored, direct operation and existing device/track cleanup boundaries before normalization or mutation while preserving attribution, device order, parameter descriptors, endpoints, levels, flags and semantic inverses without making parameters entities or adding routing/graph policy.
- Add bounded QA-01 `project.settings.update` inverse properties: enumerate declared per-field values, preserve untouched fields, prove semantic forward/inverse/forward replay and verify stacked partial updates undo and redo in strict order without changing reducer behavior.
- Add a bounded AUD-02 exclusive-playback source contract: retain shared WASAPI as the default, expose explicit exclusive opt-in at the native service boundary, configure exactly the requested miniaudio share mode without fallback, and validate requested/effective mode through protocol and host status diagnostics. Real-device acceptance remains pending.
- Prepare a separately authorized two-phase AUD-02 native checkpoint: build/unit work is isolated below one absent retained root and derives an exact executable identity; a later hash-bound device phase tests default shared plus explicit exclusive success or precise rejection with bounded audio, natural exit, no fallback, no force and no cleanup.

The owner/privacy/save/discovery changes are packaged as immutable executable `9456A999AF1DEA3910A3AA81D42E0CD136C721957038E6E3F9138CDBB69B32BC`. Its fresh independent Level 1 run is `BLOCKED`, not FAIL: launch-free gates and live cold-discovery/owner-privacy/out-of-root no-write behavior passed with no P0–P3 product finding, while UI approval, in-root save and `file.saved` invariants remain unearned solely because the Computer Use bridge could not expose a safe PID-bound target. The credential lifecycle and leakage changes are packaged separately as immutable executable `9F82CC2C52E5188B636237165B5F849F879CB540F6131227A3CFF736F6AA758C`; its independent Level 1 run is likewise infrastructure-`BLOCKED`, not FAIL, before mutation solely because exact-handle Computer Use rehydration failed. Launch-free gates and coordinator identity/window health passed, cleanup was stopped-redacted, and no P0–P3 product finding occurred; real provider DPAPI/live Electron leakage remains unearned. A separate protected-root AGT-04 headless restart acceptance passed twice on this exact hash but is not a certificate. Formal certificates remain executable-hash-scoped; the latest complete PASS remains `4CF335C1787F91C036F17FE3AA4A2F6309463F231D306A688B86A43633B16EC7`.
